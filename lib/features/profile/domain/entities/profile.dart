import 'package:chattrix_ui/features/auth/domain/entities/user.dart';

/// Profile is now just an alias to User
/// The API merged UserResponse and UserProfileResponse into one schema
typedef Profile = User;
