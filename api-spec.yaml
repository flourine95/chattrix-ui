openapi: 3.0.1
info:
  title: Chattrix API
  description: |
    API toàn diện cho ứng dụng Chattrix, bao gồm xác thực, quản lý người dùng, và tính năng gọi điện thời gian thực.

    ---

    ### JSON Serialization Behavior
    
    **Important:** API sử dụng Jackson với `JsonInclude.Include.NON_NULL` configuration.
    
    **Điều này có nghĩa:**
    - Các field có giá trị `null` sẽ KHÔNG được include trong JSON response
    - Nếu một field được đánh dấu `nullable: true` trong schema, field đó có thể null nhưng sẽ bị omit khỏi response
    - Chỉ các field có giá trị non-null mới xuất hiện trong response JSON
    
    **Ví dụ:**
    ```json
    // User object trong code có: avatarUrl = null, bio = null
    // Response JSON sẽ là:
    {
      "id": 1,
      "username": "linhla",
      "email": "linhla@chattrix.com"
      // avatarUrl và bio KHÔNG xuất hiện trong response
    }
    ```

    ---

    ### Chi tiết các sự kiện WebSocket
    Tất cả các tin nhắn từ server đều được bọc trong một đối tượng `WebSocketMessage` chung.
    
    **Message Events:**
    *   **`chat.message`**: Tin nhắn trò chuyện mới. (Payload: `OutgoingMessageDto`)
    *   **`message.updated`**: Một tin nhắn đã được cập nhật. (Payload: `MessageUpdateDto`)
    *   **`message.deleted`**: Một tin nhắn đã bị xóa. (Payload: `MessageDeleteDto`)
    *   **`message.reaction`**: Reaction đã được thêm/xóa khỏi tin nhắn. (Payload: `ReactionEventDto`)
    
    **Conversation Events:**
    *   **`conversation.update`**: Thông tin cuộc trò chuyện đã được cập nhật (ví dụ: tin nhắn cuối cùng). (Payload: `ConversationUpdateDto`)
    *   **`typing.indicator`**: User đang typing trong conversation. (Payload: `TypingIndicatorResponseDto`)
    
    **User Events:**
    *   **`user.status`**: Trạng thái trực tuyến của người dùng đã thay đổi. (Payload: `UserStatusDto`)
    
    **Friend Request Events:**
    *   **`friend.request.received`**: Nhận được friend request mới. (Payload: `FriendRequestResponse`)
    *   **`friend.request.accepted`**: Friend request được chấp nhận. (Payload: `FriendRequestResponse`)
    *   **`friend.request.rejected`**: Friend request bị từ chối. (Payload: `FriendRequestRejectDto`)
    *   **`friend.request.cancelled`**: Friend request bị hủy bởi người gửi. (Payload: `FriendRequestCancelDto`)
    
    **Note Events:**
    *   **`note.created`**: Note mới được tạo từ contact. (Payload: `UserNoteDto`)
    *   **`note.updated`**: Note của contact được cập nhật. (Payload: `UserNoteDto`)
    *   **`note.deleted`**: Note của contact bị xóa. (Payload: `NoteDeleteDto`)
    
    **Call Events:**
    *   **`call.incoming`**: Thông báo có cuộc gọi đến. (Payload: `CallInvitationDto`)
    *   **`call.accepted`**: Cuộc gọi đã được chấp nhận. (Payload: `CallAcceptDto`)
    *   **`call.rejected`**: Cuộc gọi đã bị từ chối. (Payload: `CallRejectDto`)
    *   **`call.ended`**: Cuộc gọi đã kết thúc. (Payload: `CallEndDto`)
    *   **`call.timeout`**: Cuộc gọi hết thời gian chờ. (Payload: `CallTimeoutDto`)
  version: "1.0.0"
servers:
  - url: /api
    description: Server API chính

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    # --- Auth & User Schemas ---
    LoginRequest:
      type: object
      required: [usernameOrEmail, password]
      properties:
        usernameOrEmail: { type: string, description: "Tên đăng nhập hoặc email của người dùng." }
        password: { type: string, format: password, description: "Mật khẩu." }

    RegisterRequest:
      type: object
      required: [username, email, password, fullName]
      properties:
        username:
          type: string
          minLength: 4
          maxLength: 20
          pattern: '^(?![_.])(?!.*[_.]{2})(?=.*[a-zA-Z])[a-zA-Z0-9._]+(?<![_.])$'
          description: "Username (4-20 chars, must contain letter, no leading/trailing . or _)"
        email:
          type: string
          format: email
          description: "Valid email address"
        password:
          type: string
          format: password
          minLength: 6
          maxLength: 100
          description: "Password (min 6 characters)"
        fullName:
          type: string
          minLength: 1
          maxLength: 100
          description: "Full name"

    RefreshTokenRequest:
      type: object
      required: [refreshToken]
      properties:
        refreshToken:
          type: string
          description: "Refresh token từ login/refresh response"

    ChangePasswordRequest:
      type: object
      required: [currentPassword, newPassword]
      properties:
        currentPassword:
          type: string
          description: "Current password"
        newPassword:
          type: string
          minLength: 6
          maxLength: 100
          description: "New password (min 6 characters)"

    VerifyEmailRequest:
      type: object
      required: [email, otp]
      properties:
        email:
          type: string
          format: email
          description: "Email address to verify"
        otp:
          type: string
          pattern: '^[0-9]{6}$'
          description: "6-digit OTP from email"

    ResendVerificationRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
          description: "Email address to resend verification"

    ForgotPasswordRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
          description: "Email address for password reset"

    ResetPasswordRequest:
      type: object
      required: [email, otp, newPassword]
      properties:
        email:
          type: string
          format: email
          description: "Email address"
        otp:
          type: string
          pattern: '^[0-9]{6}$'
          description: "6-digit OTP from email"
        newPassword:
          type: string
          minLength: 6
          maxLength: 100
          description: "New password (min 6 characters)"

    AuthResponse:
      type: object
      properties:
        accessToken:
          type: string
          description: "JWT access token"
        refreshToken:
          type: string
          description: "Refresh token UUID"
        tokenType:
          type: string
          example: "Bearer"
          default: "Bearer"
        expiresIn:
          type: integer
          format: int64
          description: "Access token expiry time in seconds"
          example: 86400

    UserResponse:
      type: object
      description: |
        Complete user information (same as UserProfileResponse).
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        id: { type: integer, format: int64 }
        username: { type: string }
        email: { type: string, format: email }
        emailVerified: { type: boolean }
        phone: { type: string, nullable: true }
        fullName: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        bio: { type: string, maxLength: 500, nullable: true }
        gender:
          type: string
          enum: [MALE, FEMALE, OTHER]
          nullable: true
        dateOfBirth: { type: string, format: date-time, nullable: true }
        location: { type: string, nullable: true }
        profileVisibility:
          type: string
          enum: [PUBLIC, FRIENDS_ONLY, PRIVATE]
          nullable: true
        online: { type: boolean }
        lastSeen: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time, nullable: true }

    UserProfileResponse:
      deprecated: true
      description: "DEPRECATED: Use UserResponse instead. This schema is identical to UserResponse."
      allOf:
        - $ref: '#/components/schemas/UserResponse'

    # --- Message Schemas ---
    MessageResponse:
      type: object
      description: |
        Complete message information with all metadata.
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        id: { type: integer, format: int64 }
        conversationId: { type: integer, format: int64 }
        senderId: { type: integer, format: int64 }
        senderUsername: { type: string, nullable: true }
        senderFullName: { type: string, nullable: true }
        content: { type: string, nullable: true }
        type: { type: string, enum: [TEXT, IMAGE, VIDEO, VOICE, AUDIO, DOCUMENT, LOCATION, SYSTEM], description: "Message type" }
        # Rich media fields
        mediaUrl: { type: string, format: uri, nullable: true }
        thumbnailUrl: { type: string, format: uri, nullable: true }
        fileName: { type: string, nullable: true }
        fileSize: { type: integer, format: int64, nullable: true }
        duration: { type: integer, nullable: true, description: "Duration in seconds for audio/video" }
        # Location fields
        latitude: { type: number, format: double, nullable: true }
        longitude: { type: number, format: double, nullable: true }
        locationName: { type: string, nullable: true }
        # Reply context
        replyToMessageId: { type: integer, format: int64, nullable: true }
        replyToMessage:
          type: object
          nullable: true
          properties:
            id: { type: integer, format: int64 }
            content: { type: string }
            senderId: { type: integer, format: int64 }
            senderUsername: { type: string }
        # Reactions: {"👍": [1, 2, 3], "❤️": [4, 5]}
        reactions:
          type: object
          description: "Map of emoji to array of user IDs"
          additionalProperties:
            type: array
            items:
              type: integer
              format: int64
        # Mentions
        mentions:
          type: array
          nullable: true
          items:
            type: integer
            format: int64
        mentionedUsers:
          type: array
          nullable: true
          items:
            type: object
            properties:
              userId: { type: integer, format: int64 }
              username: { type: string }
              fullName: { type: string }
        # Timestamps
        sentAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        # Edit/Delete/Forward
        edited: { type: boolean, default: false }
        editedAt: { type: string, format: date-time, nullable: true }
        deleted: { type: boolean, default: false }
        deletedAt: { type: string, format: date-time, nullable: true }
        forwarded: { type: boolean, default: false }
        originalMessageId: { type: integer, format: int64, nullable: true }
        forwardCount: { type: integer, format: int32, default: 0 }
        # Read receipts
        readCount: { type: integer, format: int64, default: 0 }
        readBy:
          type: array
          nullable: true
          items:
            type: object
            properties:
              userId: { type: integer, format: int64 }
              username: { type: string }
              fullName: { type: string }
              avatarUrl: { type: string }
              readAt: { type: string, format: date-time }
      example:
        id: 5
        conversationId: 1
        senderId: 2
        senderUsername: "user2"
        senderFullName: "Tran Van Binh"
        content: "Hì hì đúng thật, cũng đã nhiều năm trôi qua"
        type: "TEXT"
        reactions: {}
        sentAt: "2025-12-14T06:18:25.432835Z"
        createdAt: "2025-12-14T06:18:25.432836Z"
        updatedAt: "2025-12-14T06:18:25.432837Z"
        edited: false
        deleted: false
        forwarded: false
        forwardCount: 0
        readCount: 1
        readBy:
          - userId: 1
            username: "user1"
            fullName: "Nguyen Linh La"
            avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
            readAt: "2025-12-14T06:50:24.557Z"

    ChatMessageRequest:
      type: object
      required: [content]
      properties:
        content: { type: string }
        type: { type: string, enum: [TEXT, IMAGE, VIDEO, VOICE, AUDIO, DOCUMENT, LOCATION, SYSTEM], default: TEXT }
        replyToMessageId: { type: integer, format: int64, nullable: true }
        mentions:
          type: array
          items:
            type: integer
            format: int64
        mediaUrl: { type: string, format: uri, nullable: true }
        thumbnailUrl: { type: string, format: uri, nullable: true }
        fileName: { type: string, nullable: true }
        fileSize: { type: integer, format: int64, nullable: true }
        duration: { type: integer, nullable: true }
        latitude: { type: number, format: double, nullable: true }
        longitude: { type: number, format: double, nullable: true }
        locationName: { type: string, nullable: true }

    EditMessageRequest:
      type: object
      required: [content]
      properties:
        content: { type: string, description: "Nội dung mới của tin nhắn." }

    ForwardMessageRequest:
      type: object
      required: [messageId, conversationIds]
      properties:
        messageId: { type: integer, format: int64, description: "ID của tin nhắn cần forward." }
        conversationIds:
          type: array
          items: { type: integer, format: int64 }
          description: "Danh sách conversation IDs để forward tin nhắn đến."

    # --- Call Schemas ---
    CallResponse:
      type: object
      description: |
        Call information with caller/callee details and status.
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        id: { type: string, format: uuid }
        channelId: { type: string }
        status: { type: string, enum: [INITIATING, RINGING, CONNECTING, CONNECTED, DISCONNECTING, ENDED, MISSED, REJECTED, FAILED] }
        callType: { type: string, enum: [AUDIO, VIDEO] }
        callerId: { type: integer, format: int64 }
        callerName: { type: string }
        callerAvatar: { type: string, format: uri, nullable: true }
        calleeId: { type: integer, format: int64 }
        calleeName: { type: string }
        calleeAvatar: { type: string, format: uri, nullable: true }
        createdAt: { type: string, format: date-time }
        durationSeconds: { type: integer, nullable: true }

    CallConnectionResponse:
      type: object
      properties:
        callInfo: { $ref: '#/components/schemas/CallResponse' }
        token: { type: string, description: "Token (ví dụ: Agora Token) để kết nối vào phiên gọi." }

    CallHistoryResponse:
      type: object
      description: |
        Call history entry with remote user details and call outcome.
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        id:
          type: string
          description: "Call history entry ID"
        callId:
          type: string
          description: "Original call ID"
        remoteUserId:
          type: string
          description: "ID of the other user in the call"
        remoteUserName:
          type: string
          description: "Name of the other user"
        remoteUserAvatar:
          type: string
          format: uri
          nullable: true
          description: "Avatar URL of the other user. Omitted from response if null (NON_NULL serialization)."
        callType:
          type: string
          enum: [AUDIO, VIDEO]
          description: "Type of call"
        status:
          type: string
          enum: [COMPLETED, MISSED, REJECTED, FAILED]
          description: "Final status of the call"
        direction:
          type: string
          enum: [INCOMING, OUTGOING]
          description: "Direction of the call relative to the current user"
        timestamp:
          type: string
          format: date-time
          description: "When the call occurred"
        durationSeconds:
          type: integer
          nullable: true
          description: "Duration of the call in seconds. Null for missed/rejected/failed calls. Omitted from response if null (NON_NULL serialization)."

    # --- Generic API Response Wrappers ---
    ApiResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { type: object, nullable: true }

    ErrorResponse:
      type: object
      required:
        - success
        - message
        - code
        - requestId
      properties:
        success:
          type: boolean
          example: false
          description: "Always false for error responses"
        message:
          type: string
          description: "Human-readable error message. For validation errors, this is 'Validation failed'"
          example: "Invalid or expired token"
        code:
          type: string
          description: "Error code (e.g., VALIDATION_ERROR, UNAUTHORIZED, RESOURCE_NOT_FOUND, BAD_REQUEST)"
          example: "UNAUTHORIZED"
        details:
          type: object
          description: "Map of field names to error messages (only present for validation errors)"
          additionalProperties:
            type: string
          nullable: true
          example:
            email: "Email already exists"
            username: "Username must be 4-20 characters"
        requestId:
          type: string
          description: "Unique request ID for tracking"
          example: "a1b2c3d4-e5f6-4789-a012-3456789abcde"


    ApiResponse_AuthResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/AuthResponse' }

    ApiResponse_UserResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/UserResponse' }
    
    ApiResponse_UserListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          type: array
          items:
            $ref: '#/components/schemas/UserResponse'

    ApiResponse_MessageResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/MessageResponse' }

    ApiResponse_MessageListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          type: array
          items:
            $ref: '#/components/schemas/MessageResponse'

    ApiResponse_CallConnectionResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/CallConnectionResponse' }

    ApiResponse_CallResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/CallResponse' }

    ApiResponse_CallHistoryResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/PaginatedCallHistoryResponse' }

    # --- Request Schemas ---
    InitiateCallRequest:
      type: object
      required: [calleeId, callType]
      properties:
        calleeId: { type: integer, format: int64 }
        callType: { type: string, enum: [AUDIO, VIDEO] }

    RejectCallRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, enum: [busy, declined, unavailable] }

    EndCallRequest:
      type: object
      properties:
        reason: { type: string, enum: [hangup, "network error", "device error", timeout], default: "hangup" }

    # --- WebSocket DTOs ---
    OutgoingMessageDto:
      type: object
      description: "WebSocket payload for new chat message (event: chat.message)"
      properties:
        id: { type: integer, format: int64 }
        conversationId: { type: integer, format: int64 }
        sender:
          type: object
          properties:
            id: { type: integer, format: int64 }
            username: { type: string }
            fullName: { type: string }
            avatarUrl: { type: string, format: uri, nullable: true }
        content: { type: string }
        type: { type: string, enum: [TEXT, IMAGE, VIDEO, VOICE, AUDIO, DOCUMENT, LOCATION, SYSTEM] }
        createdAt: { type: string, format: date-time }
        mediaUrl: { type: string, format: uri, nullable: true }
        thumbnailUrl: { type: string, format: uri, nullable: true }
        fileName: { type: string, nullable: true }
        fileSize: { type: integer, format: int64, nullable: true }
        duration: { type: integer, nullable: true }
        latitude: { type: number, format: double, nullable: true }
        longitude: { type: number, format: double, nullable: true }
        locationName: { type: string, nullable: true }
        replyToMessageId: { type: integer, format: int64, nullable: true }
        replyToMessage:
          type: object
          nullable: true
          properties:
            id: { type: integer, format: int64 }
            content: { type: string }
            senderId: { type: integer, format: int64 }
            senderUsername: { type: string }
        reactions:
          type: object
          description: "Map of emoji to array of user IDs"
          additionalProperties:
            type: array
            items:
              type: integer
              format: int64
        mentions:
          type: array
          nullable: true
          items:
            type: integer
            format: int64
        mentionedUsers:
          type: array
          nullable: true
          items:
            type: object
            properties:
              userId: { type: integer, format: int64 }
              username: { type: string }
              fullName: { type: string }

    MessageUpdateDto:
      type: object
      description: "WebSocket payload for message update (event: message.updated)"
      properties:
        messageId: { type: integer, format: int64 }
        conversationId: { type: integer, format: int64 }
        content: { type: string }
        edited: { type: boolean }
        updatedAt: { type: string, format: date-time }

    MessageDeleteDto:
      type: object
      description: "WebSocket payload for message deletion (event: message.deleted)"
      properties:
        messageId: { type: integer, format: int64 }
        conversationId: { type: integer, format: int64 }

    ConversationUpdateDto:
      type: object
      description: "WebSocket payload for conversation update (event: conversation.update)"
      properties:
        conversationId: { type: integer, format: int64 }
        updatedAt: { type: string, format: date-time }
        lastMessage:
          type: object
          properties:
            id: { type: integer, format: int64 }
            content: { type: string }
            senderId: { type: integer, format: int64 }
            senderUsername: { type: string }
            sentAt: { type: string, format: date-time }
            type: { type: string }

    ReactionEventDto:
      type: object
      description: "WebSocket payload for reaction events (event: message.reaction)"
      properties:
        messageId: { type: integer, format: int64 }
        userId: { type: integer, format: int64 }
        userName: { type: string }
        emoji: { type: string }
        action: { type: string, enum: [add, remove] }
        reactions:
          type: object
          description: "Updated reactions map"
          additionalProperties:
            type: array
            items:
              type: integer
              format: int64
        timestamp: { type: string, format: date-time }

    TypingIndicatorDto:
      type: object
      description: "Client-to-server payload for typing indicator (send to server)"
      properties:
        conversationId: { type: integer, format: int64 }
        typing: { type: boolean, description: "true = start typing, false = stop typing" }

    TypingIndicatorResponseDto:
      type: object
      description: "WebSocket payload for typing indicator (event: typing.indicator)"
      properties:
        conversationId: { type: integer, format: int64 }
        typingUsers:
          type: array
          items:
            type: object
            properties:
              userId: { type: integer, format: int64 }
              username: { type: string }
              fullName: { type: string }

    UserStatusDto:
      type: object
      description: "WebSocket payload for user status change (event: user.status)"
      properties:
        userId: { type: string }
        username: { type: string }
        fullName: { type: string }
        online: { type: boolean }
        lastSeen: { type: string, format: date-time, nullable: true }

    FriendRequestRejectDto:
      type: object
      description: "WebSocket payload for friend request rejected (event: friend.request.rejected)"
      properties:
        requestId: { type: integer, format: int64 }
        rejectedBy: { type: integer, format: int64 }

    FriendRequestCancelDto:
      type: object
      description: "WebSocket payload for friend request cancelled (event: friend.request.cancelled)"
      properties:
        requestId: { type: integer, format: int64 }
        cancelledBy: { type: integer, format: int64 }

    CallInvitationDto:
      type: object
      description: "WebSocket payload for incoming call (event: call.incoming)"
      properties:
        callId: { type: string }
        channelId: { type: string }
        callerId: { type: integer, format: int64 }
        callerName: { type: string }
        callerAvatar: { type: string, format: uri, nullable: true }
        callType: { type: string, enum: [AUDIO, VIDEO] }

    CallAcceptDto:
      type: object
      description: "WebSocket payload for call accepted (event: call.accepted)"
      properties:
        callId: { type: string }
        acceptedBy: { type: integer, format: int64 }

    CallRejectDto:
      type: object
      description: "WebSocket payload for call rejected (event: call.rejected)"
      properties:
        callId: { type: string }
        rejectedBy: { type: integer, format: int64 }
        reason: { type: string, enum: [busy, declined, unavailable] }

    CallEndDto:
      type: object
      description: "WebSocket payload for call ended (event: call.ended)"
      properties:
        callId: { type: string }
        endedBy: { type: integer, format: int64 }
        durationSeconds: { type: integer }

    CallTimeoutDto:
      type: object
      description: "WebSocket payload for call timeout (event: call.timeout)"
      properties:
        callId: { type: string }
        reason: { type: string, example: "no_answer" }


    # --- Contacts & Social Schemas ---
    ContactResponse:
      type: object
      description: |
        Contact information with user details and relationship metadata.
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        id: { type: integer, format: int64 }
        contactUserId: { type: integer, format: int64 }
        username: { type: string }
        fullName: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        nickname: { type: string, nullable: true }
        favorite: { type: boolean }
        online: { type: boolean }
        lastSeen: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
      example:
        id: 5
        contactUserId: 10
        username: "user10"
        fullName: "Nguyen Van A"
        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
        nickname: "Best Friend"
        favorite: true
        online: true
        lastSeen: "2025-12-14T08:30:15.123Z"
        createdAt: "2025-12-10T10:20:30.456Z"

    AddContactRequest:
      type: object
      required: [contactUserId]
      properties:
        contactUserId:
          type: integer
          format: int64
          description: "User ID của người cần add vào contacts"
        nickname:
          type: string
          nullable: true
          description: "Nickname cho contact (optional)"

    UpdateContactRequest:
      type: object
      properties:
        nickname:
          type: string
          nullable: true
          description: "Update nickname"
        isFavorite:
          type: boolean
          nullable: true
          description: "Mark as favorite hoặc unfavorite"

    FriendRequestResponse:
      type: object
      properties:
        id: { type: integer, format: int64 }
        userId: { type: integer, format: int64, description: "ID của người gửi/nhận (tùy context)" }
        username: { type: string }
        fullName: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        status: { type: string, enum: [PENDING, ACCEPTED, REJECTED, BLOCKED] }
        nickname: { type: string, nullable: true }
        online: { type: boolean }
        requestedAt: { type: string, format: date-time }
        acceptedAt: { type: string, format: date-time, nullable: true }
        rejectedAt: { type: string, format: date-time, nullable: true }
      example:
        id: 15
        userId: 20
        username: "user20"
        fullName: "Tran Thi B"
        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/20.png"
        status: "PENDING"
        nickname: null
        online: false
        requestedAt: "2025-12-14T07:00:00.000Z"
        acceptedAt: null
        rejectedAt: null

    SendFriendRequestRequest:
      type: object
      required: [receiverUserId]
      properties:
        receiverUserId:
          type: integer
          format: int64
          description: "User ID của người nhận friend request"
        nickname:
          type: string
          nullable: true
          description: "Nickname cho friend (optional)"

    # --- Read Receipts Schemas ---
    ReadReceiptResponse:
      type: object
      properties:
        userId: { type: integer, format: int64 }
        username: { type: string }
        fullName: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        readAt: { type: string, format: date-time }

    # --- Reaction Schemas ---
    AddReactionRequest:
      type: object
      required: [emoji]
      properties:
        emoji: { type: string, maxLength: 10 }

    MessageReactionsResponse:
      type: object
      properties:
        messageId: { type: integer, format: int64 }
        reactions:
          type: object
          additionalProperties:
            type: array
            items:
              type: integer
              format: int64
          description: "Map of emoji to array of user IDs. Example: {'👍': [1, 2, 3], '❤️': [4, 5]}"

    # --- Conversation Members Schemas ---
    ConversationParticipantResponse:
      type: object
      properties:
        id: { type: integer, format: int64 }
        user: { $ref: '#/components/schemas/UserResponse' }
        role: { type: string, enum: [ADMIN, MEMBER] }
        joinedAt: { type: string, format: date-time }
        unreadCount: { type: integer }

    ConversationMemberResponse:
      type: object
      properties:
        id: { type: integer, format: int64 }
        fullName: { type: string }
        username: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        online: { type: boolean }

    AddMembersResponse:
      type: object
      properties:
        conversationId: { type: integer, format: int64 }
        addedMembers:
          type: array
          items:
            type: object
            properties:
              userId: { type: integer, format: int64 }
              username: { type: string }
              fullName: { type: string }
              role: { type: string, enum: [ADMIN, MEMBER] }
              joinedAt: { type: string, format: date-time }

    AddMembersRequest:
      type: object
      required: [userIds]
      properties:
        userIds:
          type: array
          items:
            type: integer
            format: int64

    UpdateMemberRoleRequest:
      type: object
      required: [role]
      properties:
        role: { type: string, enum: [ADMIN, MEMBER] }

    # --- Conversation Schemas ---
    ConversationResponse:
      type: object
      description: |
        Complete conversation information with participants and last message.
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        id: { type: integer, format: int64 }
        name: { type: string, nullable: true, description: "Tên conversation (chỉ có trong GROUP)" }
        type: { type: string, enum: [DIRECT, GROUP] }
        avatarUrl: { type: string, format: uri, nullable: true, description: "Avatar URL (chỉ có trong GROUP)" }
        participants:
          type: array
          items:
            type: object
            properties:
              userId: { type: integer, format: int64 }
              username: { type: string }
              role: { type: string, enum: [ADMIN, MEMBER] }
          description: "Danh sách participants với userId, username và role"
        lastMessage:
          type: object
          nullable: true
          properties:
            id: { type: integer, format: int64 }
            content: { type: string }
            senderId: { type: integer, format: int64 }
            senderUsername: { type: string }
            sentAt: { type: string, format: date-time }
            type: { type: string, enum: [TEXT, IMAGE, VIDEO, VOICE, AUDIO, DOCUMENT, LOCATION, SYSTEM] }
            readCount: { type: integer, format: int64 }
            readBy:
              type: array
              items:
                type: object
                properties:
                  userId: { type: integer, format: int64 }
                  username: { type: string }
                  fullName: { type: string }
                  avatarUrl: { type: string }
                  readAt: { type: string, format: date-time }
          description: "Last message in the conversation, or null if empty."
        unreadCount: { type: integer, description: "Số tin nhắn chưa đọc" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      example:
        id: 1
        type: "DIRECT"
        createdAt: "2025-12-14T05:57:39.801682Z"
        updatedAt: "2025-12-14T06:29:24.717139Z"
        participants:
          - userId: 1
            username: "user1"
            role: "ADMIN"
          - userId: 2
            username: "user2"
            role: "MEMBER"
        lastMessage:
          id: 8
          content: "Tao phải đỡ cho em tao"
          senderId: 2
          senderUsername: "user2"
          sentAt: "2025-12-14T06:29:24.691046Z"
          type: "TEXT"
          readCount: 0
          readBy: []
        unreadCount: 4

    CreateConversationRequest:
      type: object
      properties:
        name: { type: string, nullable: true }
        type: { type: string, enum: [DIRECT, GROUP], default: DIRECT }
        participantIds:
          type: array
          items:
            type: integer
            format: int64

    UpdateConversationRequest:
      type: object
      properties:
        name: { type: string, nullable: true }
        avatarUrl: { type: string, format: uri, nullable: true }

    UpdateConversationSettingsRequest:
      type: object
      properties:
        notificationsEnabled: { type: boolean, nullable: true, description: "Bật/tắt thông báo cho conversation." }
        customNickname: { type: string, nullable: true, description: "Nickname tùy chỉnh cho conversation." }
        theme: { type: string, nullable: true, description: "Theme màu cho conversation." }

    MuteConversationRequest:
      type: object
      properties:
        duration:
          type: integer
          nullable: true
          description: "Thời gian mute (giây). null/0: unmute, số dương: mute trong khoảng thời gian, -1: mute vô thời hạn."

    ConversationSettingsResponse:
      type: object
      description: |
        Conversation settings for the current user.
        Note: Fields marked as nullable will be omitted from the JSON response if their value is null (Jackson NON_NULL serialization).
      properties:
        conversationId: { type: integer, format: int64, description: "ID của conversation" }
        isMuted: { type: boolean, nullable: true, description: "Conversation có bị mute không. Omitted if null." }
        mutedUntil: { type: string, format: date-time, nullable: true, description: "Thời điểm hết mute. Omitted if null." }
        isBlocked: { type: boolean, nullable: true, description: "User trong conversation có bị block không (chỉ áp dụng cho DIRECT). Omitted if null." }
        notificationsEnabled: { type: boolean, nullable: true, description: "Thông báo có được bật không. Omitted if null." }
        customNickname: { type: string, nullable: true, description: "Nickname tùy chỉnh cho conversation. Omitted if null." }
        theme: { type: string, nullable: true, description: "Theme màu cho conversation. Omitted if null." }
      example:
        conversationId: 1
        isMuted: false
        notificationsEnabled: true

    BlockUserResponse:
      type: object
      description: "Response when blocking/unblocking a user in a conversation."
      properties:
        isBlocked: { type: boolean, description: "User có bị block không" }
        blockedAt: { type: string, format: date-time, nullable: true, description: "Thời điểm block. Omitted if null (when unblocked)." }
      example:
        isBlocked: true
        blockedAt: "2025-12-16T10:30:00Z"

    # --- User Profile Schemas ---
    UpdateUserProfileRequest:
      type: object
      properties:
        username: { type: string, minLength: 3, maxLength: 50, nullable: true }
        email: { type: string, format: email, nullable: true }
        fullName: { type: string, minLength: 1, maxLength: 100, nullable: true }
        phone: { type: string, maxLength: 20, nullable: true }
        bio: { type: string, maxLength: 500, nullable: true }
        dateOfBirth: { type: string, format: date-time, nullable: true }
        gender: { type: string, enum: [MALE, FEMALE, OTHER], nullable: true }
        location: { type: string, maxLength: 100, nullable: true }
        avatarUrl: { type: string, format: uri, nullable: true }
        profileVisibility: { type: string, enum: [PUBLIC, FRIENDS_ONLY, PRIVATE], nullable: true }

    # --- API Response Wrappers ---
    ApiResponse_ContactResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/ContactResponse' }

    ApiResponse_ContactListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          type: array
          items:
            $ref: '#/components/schemas/ContactResponse'

    ApiResponse_ReadReceiptListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          type: array
          items:
            $ref: '#/components/schemas/ReadReceiptResponse'

    ApiResponse_MessageReactionsResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/MessageReactionsResponse' }

    ApiResponse_ConversationResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/ConversationResponse' }

    PaginatedResponse:
      type: object
      description: "Generic paginated response with metadata"
      properties:
        data:
          type: array
          items: {}
          description: "Array of items for current page"
        page:
          type: integer
          format: int32
          description: "Current page number (0-indexed)"
          example: 0
        size:
          type: integer
          format: int32
          description: "Page size (items per page)"
          example: 20
        total:
          type: integer
          format: int64
          description: "Total number of items across all pages"
          example: 45
        totalPages:
          type: integer
          format: int32
          description: "Total number of pages"
          example: 3
        hasNextPage:
          type: boolean
          description: "Whether there is a next page"
          example: true
        hasPrevPage:
          type: boolean
          description: "Whether there is a previous page"
          example: false

    PaginatedConversationResponse:
      allOf:
        - $ref: '#/components/schemas/PaginatedResponse'
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: '#/components/schemas/ConversationResponse'

    UserSearchResponse:
      type: object
      description: "User search result with additional context (contact status, conversation info)"
      properties:
        id: { type: integer, format: int64 }
        username: { type: string }
        email: { type: string, format: email }
        fullName: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        online: { type: boolean }
        lastSeen: { type: string, format: date-time, nullable: true }
        contact:
          type: boolean
          description: "Whether this user is in current user's contact list"
        hasConversation:
          type: boolean
          description: "Whether a conversation exists with this user"
        conversationId:
          type: integer
          format: int64
          nullable: true
          description: "Conversation ID if exists"

    PaginatedUserSearchResponse:
      allOf:
        - $ref: '#/components/schemas/PaginatedResponse'
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: '#/components/schemas/UserSearchResponse'

    PaginatedCallHistoryResponse:
      allOf:
        - $ref: '#/components/schemas/PaginatedResponse'
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: '#/components/schemas/CallHistoryResponse'

    ApiResponse_ConversationListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          $ref: '#/components/schemas/PaginatedConversationResponse'

    ApiResponse_ParticipantListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          type: array
          items:
            $ref: '#/components/schemas/ConversationParticipantResponse'

    # --- Messenger Notes Schemas ---
    UserNoteRequest:
      type: object
      required: [noteText]
      properties:
        noteText:
          type: string
          maxLength: 60
          description: "Nội dung note (tối đa 60 ký tự)."
        musicUrl:
          type: string
          format: uri
          nullable: true
          description: "URL nhạc đính kèm (optional)."
        musicTitle:
          type: string
          maxLength: 100
          nullable: true
          description: "Tên bài nhạc (optional)."
        emoji:
          type: string
          maxLength: 10
          nullable: true
          description: "Emoji đại diện (optional)."

    UserNoteResponse:
      type: object
      properties:
        id: { type: integer, format: int64 }
        userId: { type: integer, format: int64 }
        username: { type: string }
        fullName: { type: string }
        avatarUrl: { type: string, format: uri, nullable: true }
        noteText: { type: string, maxLength: 60 }
        musicUrl: { type: string, format: uri, nullable: true }
        musicTitle: { type: string, nullable: true }
        emoji: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time, description: "Note tự động xóa sau 24h." }
        replyCount: { type: integer, format: int64, description: "Tổng số messages (replies + reactions)." }

    UserNoteDto:
      type: object
      description: "WebSocket payload cho note events."
      properties:
        id: { type: integer, format: int64 }
        userId: { type: integer, format: int64 }
        noteText: { type: string }
        emoji: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }

    NoteDeleteDto:
      type: object
      properties:
        userId: { type: integer, format: int64 }

    ApiResponse_UserNoteResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data: { $ref: '#/components/schemas/UserNoteResponse' }

    ApiResponse_UserNoteListResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        message: { type: string }
        data:
          type: array
          items:
            $ref: '#/components/schemas/UserNoteResponse'

security:
  - bearerAuth: []

tags:
  - name: Authentication
    description: "Các API liên quan đến xác thực, đăng nhập, đăng ký và quản lý phiên làm việc."
  - name: Users
    description: "Các API liên quan đến quản lý người dùng."
  - name: Conversations
    description: "Các API để quản lý cuộc trò chuyện."
  - name: Messages
    description: "Các API để quản lý tin nhắn trong cuộc trò chuyện."
  - name: Typing Indicators
    description: "Các API để quản lý typing indicators trong cuộc trò chuyện."
  - name: Notes
    description: "Các API để quản lý Messenger Notes (status 24h trên avatar)."
  - name: Contacts
    description: "Các API để quản lý danh bạ và bạn bè."
  - name: Social
    description: "Các API cho tính năng xã hội (friend requests, etc)."
  - name: Call
    description: "Các API để quản lý tính năng gọi điện."

paths:
  # --- Auth Paths ---
  /v1/auth/register:
    post:
      summary: "User Registration"
      description: |
        Đăng ký tài khoản người dùng mới.
        
        **Flow:**
        1. Validate input (username, email, password, fullName)
        2. Create user account
        3. Generate avatar automatically from Cloudinary
        4. Send verification email with 6-digit OTP
        5. User must verify email before login
        
        **Rate Limit:** 3 requests per 5 minutes
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegisterRequest' }
            example:
              username: "newuser123"
              email: "newuser@example.com"
              password: "SecurePass123"
              fullName: "New User"
      responses:
        '201':
          description: "Registration successful. Verification email sent."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Registration successful. Please check your email to verify your account."
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                username_taken:
                  summary: "Username already exists"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      username: "Username already exists"
                    requestId: "req-reg001"
                email_taken:
                  summary: "Email already registered"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      email: "Email already exists"
                    requestId: "req-reg002"
                multiple_validation_errors:
                  summary: "Multiple validation errors"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      email: "Email already exists"
                      username: "Username already exists"
                    requestId: "req-reg003"
                invalid_username:
                  summary: "Invalid username format"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      username: "Username must contain at least one letter, and cannot start or end with '.' or '_'"
                    requestId: "req-reg004"
                username_too_short:
                  summary: "Username too short"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      username: "Username must be between 4 and 20 characters"
                    requestId: "req-reg005"
                invalid_email:
                  summary: "Invalid email format"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      email: "Email must be valid"
                    requestId: "req-reg006"
                password_too_short:
                  summary: "Password too short"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      password: "Password must be at least 6 characters"
                    requestId: "req-reg007"
                missing_fullname:
                  summary: "Full name missing"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      fullName: "Full name cannot be blank"
                    requestId: "req-reg008"
        '429':
          description: "Too Many Requests - Rate limit exceeded"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Too many registration attempts. Please try again later."
                code: "RATE_LIMIT_EXCEEDED"
                requestId: "req-reg009"

  /v1/auth/login:
    post:
      summary: "User Login"
      description: |
        Xác thực người dùng để nhận access token và refresh token.
        
        **Flow:**
        1. Validate credentials (username/email + password)
        2. Check email verified
        3. Mark user as online
        4. Generate access token (JWT) và refresh token
        5. Return tokens
        
        **Rate Limit:** 10 requests per 60 seconds
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/LoginRequest' }
            examples:
              with_username:
                summary: "Login with username"
                value:
                  usernameOrEmail: "user1"
                  password: "password123"
              with_email:
                summary: "Login with email"
                value:
                  usernameOrEmail: "user1@example.com"
                  password: "password123"
      responses:
        '200':
          description: "Login successful. Tokens returned."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/AuthResponse' }
              example:
                success: true
                message: "Login successful"
                data:
                  accessToken: "eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxIiwidXNlcm5hbWUiOiJ1c2VyMSIsImp0aSI6IjVlYjRmZTAzLTNkMjMtNDcwNy1iN2I5LTU2MDE1MDE2M2RiOCIsImlhdCI6MTc2NTc4MDQxNiwiZXhwIjoxNzY1OTk2NDE2fQ.0Dh05v6LLMN0WozkhPqHfkds1Gb2JNXZi-suzBUYK5A"
                  refreshToken: "b6b44509-3999-4737-87c7-b5214e0d925f"
                  tokenType: "Bearer"
                  expiresIn: 216000
        '400':
          description: "Bad Request - Missing fields."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              example:
                success: false
                message: "Validation failed"
                code: "VALIDATION_ERROR"
                details:
                  usernameOrEmail: "Username or email cannot be blank"
                requestId: "req-login001"
        '401':
          description: "Unauthorized - Invalid credentials or email not verified."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                invalid_credentials:
                  summary: "Wrong username/email or password"
                  value:
                    success: false
                    message: "Invalid username/email or password"
                    code: "UNAUTHORIZED"
                    requestId: "req-login002"
                email_not_verified:
                  summary: "Email not verified"
                  value:
                    success: false
                    message: "Email not verified. Please check your email to verify your account."
                    code: "UNAUTHORIZED"
                    requestId: "req-login003"
        '429':
          description: "Too Many Requests - Rate limit exceeded"
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Too many login attempts. Please try again later."
                code: "RATE_LIMIT_EXCEEDED"
                requestId: "req-login004"

  /v1/auth/refresh:
    post:
      summary: "Refresh Access Token"
      description: |
        Sử dụng refresh token để lấy access token mới.
        
        **Flow:**
        1. Validate refresh token
        2. Invalidate old access token (if still valid)
        3. Revoke old refresh token
        4. Generate new access token và refresh token
        5. Return new tokens
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RefreshTokenRequest' }
            example:
              refreshToken: "d47f9e4c-8b2a-4e1f-9c3d-5a6b7c8d9e0f"
      responses:
        '200':
          description: "Token refreshed successfully. New tokens returned."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/AuthResponse' }
              example:
                success: true
                message: "Token refreshed successfully"
                data:
                  accessToken: "eyJhbGciOiJIUzI1NiJ9.new_token_here..."
                  refreshToken: "new_refresh_token_uuid"
                  tokenType: "Bearer"
                  expiresIn: 86400
        '401':
          description: "Unauthorized - Invalid or expired refresh token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                invalid_token:
                  summary: "Invalid refresh token"
                  value:
                    success: false
                    message: "Invalid or expired refresh token"
                    code: "UNAUTHORIZED"
                    requestId: "req-refresh001"
                token_revoked:
                  summary: "Token already revoked"
                  value:
                    success: false
                    message: "Refresh token has been revoked"
                    code: "UNAUTHORIZED"
                    requestId: "req-refresh002"

  /v1/auth/me:
    get:
      summary: "Get Current User Info"
      description: "Lấy thông tin chi tiết của người dùng đang đăng nhập, dựa trên token xác thực."
      tags: [Authentication]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "User retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/UserResponse' }
              example:
                success: true
                message: "User retrieved successfully"
                data:
                  id: 1
                  username: "user1"
                  email: "linhla@gmail.com"
                  emailVerified: true
                  phone: "0912345678"
                  fullName: "Nguyen Linh La"
                  avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                  bio: "Hello world!"
                  gender: "FEMALE"
                  dateOfBirth: "1999-01-01T00:00:00Z"
                  location: "Ho Chi Minh"
                  profileVisibility: "PUBLIC"
                  online: true
                  lastSeen: "2025-12-15T06:33:36.031646Z"
                  createdAt: "2025-12-11T18:11:24.950589Z"
                  updatedAt: "2025-12-15T06:33:36.034671Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-authme001"

  /v1/auth/logout:
    post:
      summary: "Logout from current device"
      description: |
        Logout user khỏi device hiện tại.
        
        **Actions:**
        - Mark user as offline
        - Invalidate current access token
        - Revoke associated refresh token
      tags: [Authentication]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Logged out successfully from this device."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Logged out from this device successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-logout001"

  /v1/auth/logout-all:
    post:
      summary: "Logout from all devices"
      description: |
        Logout user khỏi tất cả devices.
        
        **Actions:**
        - Mark user as offline
        - Revoke all refresh tokens
        - All existing access tokens will be invalid after expiry
      tags: [Authentication]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Logged out successfully from all devices."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Logged out from all devices successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-logoutall001"

  /v1/auth/change-password:
    put:
      summary: "Change password"
      description: |
        Thay đổi password của user hiện tại.
        
        **Validation:**
        - Current password must be correct
        - New password must be different from current
        - New password minimum 6 characters
      tags: [Authentication]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ChangePasswordRequest' }
            example:
              currentPassword: "oldPassword123"
              newPassword: "newSecurePass456"
      responses:
        '200':
          description: "Password changed successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Password changed successfully"
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                wrong_current_password:
                  summary: "Current password incorrect"
                  value:
                    success: false
                    message: "Current password is incorrect"
                    code: "BAD_REQUEST"
                    requestId: "req-changepw001"
                same_password:
                  summary: "New password same as current"
                  value:
                    success: false
                    message: "New password must be different from current password"
                    code: "BAD_REQUEST"
                    requestId: "req-changepw002"
                password_too_short:
                  summary: "New password too short"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      newPassword: "New password must be at least 6 characters"
                    requestId: "req-changepw003"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-changepw004"

  /v1/auth/verify-email:
    post:
      summary: "Verify email with OTP"
      description: |
        Xác thực email sau khi đăng ký.
        
        **Flow:**
        1. User receives 6-digit OTP via email
        2. Submit email + OTP
        3. Email marked as verified
        4. User can now login
        
        **OTP:**
        - 6 digits
        - Valid for 10 minutes
        - Rate limit: 1 request per 5 minutes
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/VerifyEmailRequest' }
            example:
              email: "user@example.com"
              otp: "123456"
      responses:
        '200':
          description: "Email verified successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Email verified successfully. You can now login."
        '400':
          description: "Bad Request - Invalid OTP or validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                invalid_otp:
                  summary: "Invalid or expired OTP"
                  value:
                    success: false
                    message: "Invalid or expired verification code"
                    code: "BAD_REQUEST"
                    requestId: "req-verify001"
                already_verified:
                  summary: "Email already verified"
                  value:
                    success: false
                    message: "Email already verified"
                    code: "BAD_REQUEST"
                    requestId: "req-verify002"
                invalid_otp_format:
                  summary: "OTP format invalid"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      otp: "OTP must be 6 digits"
                    requestId: "req-verify003"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-verify004"
        '429':
          description: "Too Many Requests - Rate limit exceeded."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Too many verification attempts. Please try again later."
                code: "RATE_LIMIT_EXCEEDED"
                requestId: "req-verify005"

  /v1/auth/resend-verification:
    post:
      summary: "Resend verification email"
      description: |
        Gửi lại email verification với OTP mới.
        
        **Rate limit:** 3 requests per 10 minutes
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ResendVerificationRequest' }
            example:
              email: "user@example.com"
      responses:
        '200':
          description: "Verification email sent successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Verification email sent successfully"
        '400':
          description: "Bad Request - Email already verified."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Email already verified"
                code: "BAD_REQUEST"
                requestId: "req-resend001"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-resend002"
        '429':
          description: "Too Many Requests - Rate limit exceeded."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Too many requests. Please try again later."
                code: "RATE_LIMIT_EXCEEDED"
                requestId: "req-resend003"

  /v1/auth/forgot-password:
    post:
      summary: "Request password reset"
      description: |
        Gửi email với OTP để reset password.
        
        **Flow:**
        1. Submit email
        2. Receive 6-digit OTP via email
        3. Use OTP with /reset-password endpoint
        
        **Rate limit:** 3 requests per 10 minutes
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ForgotPasswordRequest' }
            example:
              email: "user@example.com"
      responses:
        '200':
          description: "Password reset email sent successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Password reset email sent successfully"
        '404':
          description: "User not found (silently return 200 for security)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Password reset email sent successfully"
        '429':
          description: "Too Many Requests - Rate limit exceeded."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Too many requests. Please try again later."
                code: "RATE_LIMIT_EXCEEDED"
                requestId: "req-forgot001"

  /v1/auth/reset-password:
    post:
      summary: "Reset password with OTP"
      description: |
        Reset password sử dụng OTP từ email.
        
        **Flow:**
        1. User receives OTP from /forgot-password
        2. Submit email + OTP + new password
        3. Password is reset
        4. User can login with new password
        
        **Rate limit:** 1 request per 5 minutes
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ResetPasswordRequest' }
            example:
              email: "user@example.com"
              otp: "123456"
              newPassword: "newSecurePassword123"
      responses:
        '200':
          description: "Password reset successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Password reset successfully"
        '400':
          description: "Bad Request - Invalid OTP or validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                invalid_otp:
                  summary: "Invalid or expired OTP"
                  value:
                    success: false
                    message: "Invalid or expired reset code"
                    code: "BAD_REQUEST"
                    requestId: "req-reset001"
                password_too_short:
                  summary: "Password too short"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      newPassword: "Password must be at least 6 characters"
                    requestId: "req-reset002"
                invalid_otp_format:
                  summary: "OTP format invalid"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      otp: "OTP must be 6 digits"
                    requestId: "req-reset003"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-reset004"
        '429':
          description: "Too Many Requests - Rate limit exceeded."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Too many reset attempts. Please try again later."
                code: "RATE_LIMIT_EXCEEDED"
                requestId: "req-reset005"

  /v1/users/search:
    get:
      summary: "Search users"
      description: |
        Tìm kiếm người dùng theo username, email hoặc full name với phân trang.
        
        **Search:** Case-insensitive partial match trên username, email, fullName
        **Additional info:** Includes contact status and conversation info
        **Pagination:** Default 20 items per page
      tags: [Users]
      security:
        - bearerAuth: []
      parameters:
        - name: query
          in: query
          required: false
          schema:
            type: string
            default: ""
          description: "Search keyword (username, email, or name). Empty = all users"
          example: "nguyen"
        - name: page
          in: query
          required: false
          schema:
            type: integer
            format: int32
            minimum: 0
            default: 0
          description: "Page number (0-indexed)"
          example: 0
        - name: size
          in: query
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
          description: "Number of items per page"
          example: 20
      responses:
        '200':
          description: "Users found successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/PaginatedUserSearchResponse'
              example:
                success: true
                message: "Users found successfully"
                data:
                  data:
                    - id: 7
                      username: "lanbui"
                      email: "lanbui@gmail.com"
                      fullName: "Bui Thi Lan"
                      avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/7.png"
                      online: false
                      lastSeen: "2025-12-11T18:17:10.777888Z"
                      contact: false
                      hasConversation: false
                      conversationId: null
                    - id: 2
                      username: "user2"
                      email: "binhtran@gmail.com"
                      fullName: "Tran Van Binh"
                      avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/2.png"
                      online: false
                      lastSeen: "2025-12-14T06:12:27.806735Z"
                      contact: false
                      hasConversation: true
                      conversationId: 1
                    - id: 3
                      username: "hoale"
                      email: "hoale@gmail.com"
                      fullName: "Le Thi Hoa"
                      avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/3.png"
                      online: false
                      lastSeen: "2025-12-12T08:30:00Z"
                      contact: true
                      hasConversation: true
                      conversationId: 3
                  page: 0
                  size: 20
                  total: 19
                  totalPages: 1
                  hasNextPage: false
                  hasPrevPage: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-search002"

  /v1/users/status/batch:
    get:
      summary: "Get batch user status"
      description: |
        Lấy online status của nhiều users cùng lúc.
        
        **Use case:** Update online status for contact list, conversation participants
      tags: [Users]
      security:
        - bearerAuth: []
      parameters:
        - name: userIds
          in: query
          required: true
          schema:
            type: string
          description: "Comma-separated user IDs"
          example: "1,5,10,15"
      responses:
        '200':
          description: "User statuses retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        userId: { type: integer, format: int64 }
                        online: { type: boolean }
                        lastSeen: { type: string, format: date-time, nullable: true }
              example:
                success: true
                message: "Statuses retrieved successfully"
                data:
                  - userId: 1
                    online: true
                    lastSeen: null
                  - userId: 5
                    online: false
                    lastSeen: "2025-12-14T08:30:00.000Z"
                  - userId: 10
                    online: true
                    lastSeen: null
        '400':
          description: "Bad Request - Invalid user IDs."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              example:
                success: false
                message: "Validation failed"
                code: "VALIDATION_ERROR"
                details:
                  userIds: "User IDs parameter is required"
                requestId: "req-status001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-status002"

  /v1/users/status:
    put:
      summary: "Update user status manually"
      description: |
        Manually set online/offline status.
        
        **Note:** Usually automatic via WebSocket connect/disconnect.
        This endpoint for manual override.
      tags: [Users]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [online]
              properties:
                online:
                  type: boolean
                  description: "Set online or offline"
            example:
              online: true
      responses:
        '200':
          description: "Status updated successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Status updated successfully"
        '400':
          description: "Bad Request - Invalid status value."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              example:
                success: false
                message: "Validation failed"
                code: "VALIDATION_ERROR"
                details:
                  online: "Online field is required"
                requestId: "req-statusupd001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-statusupd002"

  /v1/profile/me:
    get:
      summary: "Get my profile"
      description: "Lấy thông tin profile của người dùng hiện tại."
      tags: [Users]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Profile retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/UserResponse' }
              example:
                success: true
                message: "Profile retrieved successfully"
                data:
                  id: 1
                  username: "user1"
                  email: "user1@example.com"
                  emailVerified: true
                  phone: "+84987654321"
                  fullName: "Nguyen Linh La"
                  avatarUrl: "https://res.cloudinary.com/.../1.png"
                  bio: "Software Developer | Tech Enthusiast"
                  gender: "MALE"
                  dateOfBirth: "1995-05-15T00:00:00.000Z"
                  location: "Ho Chi Minh City, Vietnam"
                  profileVisibility: "PUBLIC"
                  online: true
                  lastSeen: null
                  createdAt: "2025-01-01T00:00:00.000Z"
                  updatedAt: "2025-01-08T00:00:00.000Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-profile001"
    put:
      summary: "Update my profile"
      description: |
        Cập nhật thông tin profile của người dùng hiện tại.
        
        **Updatable fields:** fullName, avatarUrl, phone, bio, dateOfBirth, gender, location, profileVisibility
      tags: [Users]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateUserProfileRequest' }
            examples:
              update_name:
                summary: "Update full name"
                value:
                  fullName: "Nguyen Van New Name"
              update_avatar:
                summary: "Update avatar"
                value:
                  avatarUrl: "https://res.cloudinary.com/.../new-avatar.png"
              update_bio:
                summary: "Update bio"
                value:
                  bio: "Full-stack developer with passion for AI"
              update_all:
                summary: "Update multiple fields"
                value:
                  fullName: "Nguyen Van New Name"
                  avatarUrl: "https://res.cloudinary.com/.../new-avatar.png"
                  bio: "Full-stack developer"
                  phone: "+84987654321"
                  gender: "MALE"
                  location: "Hanoi, Vietnam"
                  profileVisibility: "FRIENDS_ONLY"
      responses:
        '200':
          description: "Profile updated successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/UserResponse' }
              example:
                success: true
                message: "Profile updated successfully"
                data:
                  id: 1
                  username: "user1"
                  email: "user1@example.com"
                  emailVerified: true
                  phone: "+84987654321"
                  fullName: "Nguyen Van New Name"
                  avatarUrl: "https://res.cloudinary.com/.../new-avatar.png"
                  bio: "Full-stack developer"
                  gender: "MALE"
                  dateOfBirth: "1995-05-15T00:00:00.000Z"
                  location: "Hanoi, Vietnam"
                  profileVisibility: "FRIENDS_ONLY"
                  online: true
                  lastSeen: null
                  createdAt: "2025-01-01T00:00:00.000Z"
                  updatedAt: "2025-01-10T10:30:00.000Z"
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                invalid_fullname:
                  summary: "Full name too long"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      fullName: "Full name must be between 1 and 100 characters"
                    requestId: "req-profileupd001"
                invalid_bio:
                  summary: "Bio too long"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      bio: "Bio must not exceed 500 characters"
                    requestId: "req-profileupd002"
                invalid_avatar_url:
                  summary: "Invalid avatar URL"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      avatarUrl: "Avatar URL must be valid"
                    requestId: "req-profileupd003"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-profileupd004"

  /v1/profile/{userId}:
    get:
      summary: "Get user profile by ID"
      description: "Lấy thông tin profile của một người dùng cụ thể."
      tags: [Users]
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "User ID"
          example: 5
      responses:
        '200':
          description: "Profile retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/UserResponse' }
              example:
                success: true
                message: "Profile retrieved successfully"
                data:
                  id: 5
                  username: "user5"
                  email: "user5@example.com"
                  fullName: "Nguyen Van A"
                  avatarUrl: "https://res.cloudinary.com/.../5.png"
                  online: false
                  lastSeen: "2025-12-14T08:30:00.000Z"
                  emailVerified: true
                  createdAt: "2025-01-05T00:00:00.000Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-profileget001"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-profileget002"

  /v1/profile/username/{username}:
    get:
      summary: "Get user profile by username"
      description: "Lấy thông tin profile của người dùng theo username."
      tags: [Users]
      security:
        - bearerAuth: []
      parameters:
        - name: username
          in: path
          required: true
          schema: { type: string }
          description: "Username"
          example: "user5"
      responses:
        '200':
          description: "Profile retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/UserResponse' }
              example:
                success: true
                message: "Profile retrieved successfully"
                data:
                  id: 5
                  username: "user5"
                  email: "user5@example.com"
                  fullName: "Nguyen Van A"
                  avatarUrl: "https://res.cloudinary.com/.../5.png"
                  online: false
                  lastSeen: "2025-12-14T08:30:00.000Z"
                  emailVerified: true
                  createdAt: "2025-01-05T00:00:00.000Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-profileusername001"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-profileusername002"

  # --- User Notes Paths ---
  /v1/notes:
    post:
      summary: "Create or update note"
      description: |
        Tạo hoặc cập nhật note (status) của người dùng hiện tại.
        
        **Rules:**
        - Mỗi user chỉ có 1 note active tại một thời điểm
        - Note tự động expire sau 24 giờ
        - Tối đa 60 ký tự
        - Emoji optional
      tags: [User Notes]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [noteText]
              properties:
                noteText:
                  type: string
                  maxLength: 60
                  description: "Note content (max 60 chars)"
                emoji:
                  type: string
                  nullable: true
                  description: "Optional emoji"
            examples:
              with_emoji:
                summary: "Note with emoji"
                value:
                  noteText: "Feeling great today!"
                  emoji: "😊"
              without_emoji:
                summary: "Note without emoji"
                value:
                  noteText: "Working on new project"
      responses:
        '201':
          description: "Note created/updated successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/UserNoteResponse' }
              example:
                success: true
                message: "Note created successfully"
                data:
                  id: 15
                  userId: 1
                  noteText: "Feeling great today!"
                  emoji: "😊"
                  createdAt: "2025-12-14T10:00:00.000Z"
                  expiresAt: "2025-12-15T10:00:00.000Z"
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                text_too_long:
                  summary: "Note text too long"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      noteText: "Note text must not exceed 60 characters"
                    requestId: "req-note001"
                text_empty:
                  summary: "Note text empty"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      noteText: "Note text cannot be blank"
                    requestId: "req-note002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-note003"
    get:
      summary: "Get my current note"
      description: "Lấy note hiện tại của người dùng."
      tags: [User Notes]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Note retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    oneOf:
                      - $ref: '#/components/schemas/UserNoteResponse'
                      - type: object
                        nullable: true
              examples:
                has_note:
                  summary: "User has active note"
                  value:
                    success: true
                    message: "Note retrieved successfully"
                    data:
                      id: 15
                      userId: 1
                      noteText: "Feeling great today!"
                      emoji: "😊"
                      createdAt: "2025-12-14T10:00:00.000Z"
                      expiresAt: "2025-12-15T10:00:00.000Z"
                no_note:
                  summary: "User has no note"
                  value:
                    success: true
                    message: "No active note"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-noteget001"
    delete:
      summary: "Delete my note"
      description: "Xóa note hiện tại của người dùng."
      tags: [User Notes]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Note deleted successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Note deleted successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-notedel001"
        '404':
          description: "Note not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Note not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-notedel002"

  /v1/notes/contacts:
    get:
      summary: "Get notes from contacts"
      description: |
        Lấy danh sách notes của tất cả contacts.
        
        **Use case:** Display notes/statuses in contact list
      tags: [User Notes]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Contact notes retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserNoteResponse'
              example:
                success: true
                message: "Contact notes retrieved successfully"
                data:
                  - id: 15
                    userId: 5
                    noteText: "On vacation 🏖️"
                    emoji: "🏖️"
                    createdAt: "2025-12-14T10:00:00.000Z"
                    expiresAt: "2025-12-15T10:00:00.000Z"
                  - id: 18
                    userId: 10
                    noteText: "Busy with meetings"
                    emoji: "📅"
                    createdAt: "2025-12-14T09:00:00.000Z"
                    expiresAt: "2025-12-15T09:00:00.000Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-contactnotes001"

  /v1/notes/user/{userId}:
    get:
      summary: "Get note of specific user"
      description: "Lấy note của một user cụ thể."
      tags: [User Notes]
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "User ID"
          example: 5
      responses:
        '200':
          description: "User note retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    oneOf:
                      - $ref: '#/components/schemas/UserNoteResponse'
                      - type: object
                        nullable: true
              examples:
                has_note:
                  summary: "User has note"
                  value:
                    success: true
                    message: "Note retrieved successfully"
                    data:
                      id: 15
                      userId: 5
                      noteText: "On vacation 🏖️"
                      emoji: "🏖️"
                      createdAt: "2025-12-14T10:00:00.000Z"
                      expiresAt: "2025-12-15T10:00:00.000Z"
                no_note:
                  summary: "User has no note"
                  value:
                    success: true
                    message: "No active note"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-usernote001"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-usernote002"

  # --- Conversation Paths ---
  /v1/conversations:
    post:
      summary: "Create a new conversation"
      description: |
        Tạo một cuộc trò chuyện mới (1-1 hoặc nhóm).
        
        **VALIDATION RULES:**
        - **DIRECT**: PHẢI có đúng 1 participant khác (ngoài current user)
          - ❌ WRONG: participantIds: [17, 18, 19] (3 người)
          - ✅ CORRECT: participantIds: [17] (1 người)
          
        - **GROUP**: PHẢI có ít nhất 1 participant khác (ngoài current user)
          - ✅ CORRECT: participantIds: [17, 18] (2+ người)
          - ✅ CORRECT: participantIds: [17] (1 người, nhưng nên dùng DIRECT thay vì GROUP)
        
        **Note**: Current user tự động được thêm vào conversation, không cần đưa vào participantIds.
      tags: [Conversations]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateConversationRequest' }
            examples:
              direct:
                summary: "Direct conversation (1-1) - CORRECT"
                value:
                  type: "DIRECT"
                  participantIds: [17]
              direct_wrong:
                summary: "Direct conversation - WRONG (multiple participants)"
                value:
                  type: "DIRECT"
                  participantIds: [17, 18, 19]
              group:
                summary: "Group conversation - CORRECT"
                value:
                  name: "Team Chat"
                  type: "GROUP"
                  participantIds: [17, 18, 19]
      responses:
        '201':
          description: "Conversation created successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ConversationResponse' }
              example:
                success: true
                message: "Conversation created successfully"
                data:
                  id: 7
                  type: "DIRECT"
                  createdAt: "2025-12-14T06:50:02.634141682Z"
                  updatedAt: "2025-12-14T06:50:02.634143143Z"
                  participants:
                    - userId: 1
                      username: "user1"
                      role: "ADMIN"
                    - userId: 15
                      username: "sontrinh"
                      role: "MEMBER"
        '400':
          description: |
            Bad Request - Validation failed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                direct_multiple_participants:
                  summary: "DIRECT conversation with multiple participants"
                  value:
                    success: false
                    message: "DIRECT conversation must have exactly 1 other participant. Found: 3"
                    code: "VALIDATION_ERROR"
                    requestId: "req-abc123"
                no_participants:
                  summary: "No participants provided"
                  value:
                    success: false
                    message: "At least one participant is required"
                    code: "VALIDATION_ERROR"
                    requestId: "req-def456"
                participant_not_found:
                  summary: "Participant user not found"
                  value:
                    success: false
                    message: "Participant not found: 99999"
                    code: "RESOURCE_NOT_FOUND"
                    requestId: "req-ghi789"
                conversation_exists:
                  summary: "Conversation already exists (DIRECT)"
                  value:
                    success: false
                    message: "A conversation with this user already exists"
                    code: "CONFLICT"
                    requestId: "req-jkl012"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-mno345"
    get:
      summary: "Get all conversations"
      description: "Lấy danh sách tất cả cuộc trò chuyện của người dùng hiện tại, sắp xếp theo tin nhắn mới nhất. Hỗ trợ filter theo loại conversation và phân trang."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: filter
          in: query
          required: false
          description: |
            Filter conversations by type:
            - **all** (default): Tất cả conversations
            - **unread**: Chỉ conversations có tin nhắn chưa đọc
            - **group**: Chỉ conversations nhóm (GROUP)
          schema:
            type: string
            enum: [all, unread, group]
            default: all
          example: all
        - name: page
          in: query
          required: false
          description: "Page number (0-indexed)"
          schema:
            type: integer
            format: int32
            minimum: 0
            default: 0
          example: 0
        - name: size
          in: query
          required: false
          description: "Number of items per page"
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 100
            default: 20
          example: 20
      responses:
        '200':
          description: "Conversations retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ConversationListResponse' }
              examples:
                all:
                  summary: "All conversations (filter=all) with pagination"
                  value:
                    success: true
                    message: "Conversations retrieved successfully"
                    data:
                      data:
                        - id: 1
                          type: "DIRECT"
                          createdAt: "2025-12-12T09:35:09.382687Z"
                          updatedAt: "2025-12-13T07:38:26.471576Z"
                          participants:
                            - userId: 12
                              username: "hungly"
                              role: "ADMIN"
                            - userId: 17
                              username: "ducphan"
                              role: "MEMBER"
                          lastMessage:
                            id: 6
                            content: "đã sửa nha"
                            senderId: 12
                            senderUsername: "hungly"
                            sentAt: "2025-12-13T07:38:26.438210Z"
                            type: "TEXT"
                        - id: 5
                          type: "GROUP"
                          createdAt: "2025-12-12T09:39:16.600310Z"
                          updatedAt: "2025-12-12T09:39:16.600313Z"
                          participants:
                            - userId: 12
                              username: "hungly"
                              role: "ADMIN"
                            - userId: 3
                              username: "hoale"
                              role: "MEMBER"
                            - userId: 4
                              username: "tuanpham"
                              role: "MEMBER"
                            - userId: 2
                              username: "binhtran"
                              role: "MEMBER"
                            - userId: 5
                              username: "thuyvo"
                              role: "MEMBER"
                        - id: 4
                          type: "DIRECT"
                          createdAt: "2025-12-12T09:38:52.749391Z"
                          updatedAt: "2025-12-12T09:38:52.749395Z"
                          participants:
                            - userId: 12
                              username: "hungly"
                              role: "ADMIN"
                            - userId: 17
                              username: "ducphan"
                              role: "MEMBER"
                      page: 0
                      size: 20
                      total: 3
                      totalPages: 1
                      hasNextPage: false
                      hasPrevPage: false
                unread:
                  summary: "Only unread conversations (filter=unread)"
                  value:
                    success: true
                    message: "Conversations retrieved successfully"
                    data:
                      data:
                        - id: 1
                          type: "DIRECT"
                          createdAt: "2025-12-12T09:35:09.382687Z"
                          updatedAt: "2025-12-13T07:38:26.471576Z"
                          participants:
                            - userId: 12
                              username: "hungly"
                              role: "ADMIN"
                            - userId: 17
                              username: "ducphan"
                              role: "MEMBER"
                          lastMessage:
                            id: 6
                            content: "đã sửa nha"
                            senderId: 12
                            senderUsername: "hungly"
                            sentAt: "2025-12-13T07:38:26.438210Z"
                            type: "TEXT"
                      page: 0
                      size: 20
                      total: 1
                      totalPages: 1
                      hasNextPage: false
                      hasPrevPage: false
                group:
                  summary: "Only group conversations (filter=group)"
                  value:
                    success: true
                    message: "Conversations retrieved successfully"
                    data:
                      data:
                        - id: 5
                          type: "GROUP"
                          createdAt: "2025-12-12T09:39:16.600310Z"
                          updatedAt: "2025-12-12T09:39:16.600313Z"
                          participants:
                            - userId: 12
                              username: "hungly"
                              role: "ADMIN"
                            - userId: 3
                              username: "hoale"
                              role: "MEMBER"
                            - userId: 4
                              username: "tuanpham"
                              role: "MEMBER"
                            - userId: 2
                              username: "binhtran"
                              role: "MEMBER"
                            - userId: 5
                              username: "thuyvo"
                              role: "MEMBER"
                      page: 0
                      size: 20
                      total: 1
                      totalPages: 1
                      hasNextPage: false
                      hasPrevPage: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "27fcc299-9b9b-4cfd-a87f-82260d7fc124"

  /v1/conversations/{conversationId}:
    get:
      summary: "Get a conversation"
      description: "Lấy thông tin chi tiết của một cuộc trò chuyện, bao gồm participants và last message."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          example: 1
      responses:
        '200':
          description: "Conversation retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ConversationResponse' }
              example:
                success: true
                message: "Conversation retrieved successfully"
                data:
                  id: 1
                  type: "DIRECT"
                  createdAt: "2025-12-12T09:35:09.382687Z"
                  updatedAt: "2025-12-13T07:38:26.471576Z"
                  participants:
                    - userId: 12
                      username: "hungly"
                      role: "ADMIN"
                    - userId: 17
                      username: "ducphan"
                      role: "MEMBER"
                  lastMessage:
                    id: 6
                    content: "đã sửa nha"
                    senderId: 12
                    senderUsername: "hungly"
                    sentAt: "2025-12-13T07:38:26.438210Z"
                    type: "TEXT"
        '400':
          description: "Bad Request - You do not have access to this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You do not have access to this conversation"
                code: "FORBIDDEN"
                requestId: "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "27fcc299-9b9b-4cfd-a87f-82260d7fc124"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "783376db-aad3-4ae8-9886-9074a79356d0"
    put:
      summary: "Update a conversation"
      description: |
        Cập nhật thông tin cuộc trò chuyện (tên, avatar).
        - Chỉ admin mới được cập nhật trong group chat
        - Không thể update direct conversation
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          example: 7
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateConversationRequest' }
            example:
              name: "day la group"
              avatarUrl: "http://TDBUQDLVNCWK.hiynU7F27tcNxQSUu-RDCu0"
      responses:
        '200':
          description: "Conversation updated successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ConversationResponse' }
              example:
                success: true
                message: "Conversation updated successfully"
                data:
                  id: 7
                  type: "GROUP"
                  name: "day la group"
                  avatarUrl: "http://TDBUQDLVNCWK.hiynU7F27tcNxQSUu-RDCu0"
                  createdAt: "2025-12-13T09:14:14.522996Z"
                  updatedAt: "2025-12-13T09:14:14.522999Z"
                  participants:
                    - userId: 5
                      username: "thuyvo"
                      role: "MEMBER"
                    - userId: 4
                      username: "tuanpham"
                      role: "MEMBER"
                    - userId: 6
                      username: "thanhdang"
                      role: "MEMBER"
                    - userId: 12
                      username: "hungly"
                      role: "ADMIN"
                    - userId: 2
                      username: "binhtran"
                      role: "MEMBER"
                    - userId: 3
                      username: "hoale"
                      role: "MEMBER"
        '400':
          description: "Bad Request - Validation failed or no access."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You do not have access to this conversation"
                code: "BAD_REQUEST"
                requestId: "b1c2d3e4-f5a6-7890-bcde-f12345678901"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "27fcc299-9b9b-4cfd-a87f-82260d7fc124"
        '403':
          description: "Forbidden - Only admin can update."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Only admin can update group conversation"
                code: "FORBIDDEN"
                requestId: "c2d3e4f5-a6b7-8901-cdef-234567890abc"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "NOT_FOUND"
                requestId: "d3e4f5a6-b7c8-9012-def0-34567890abcd"
    delete:
      summary: "Delete a conversation"
      description: |
        Xóa một cuộc trò chuyện (user rời khỏi conversation).
        - User sẽ bị remove khỏi participants list
        - Có thể áp dụng cho cả DIRECT và GROUP conversation
        - Conversation vẫn tồn tại cho các participants khác
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          example: 7
      responses:
        '200':
          description: "Conversation deleted successfully (user left the conversation)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string, example: "Conversation deleted successfully" }
              example:
                success: true
                message: "Conversation deleted successfully"
        '400':
          description: "Bad Request - You do not have access to this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You do not have access to this conversation"
                code: "BAD_REQUEST"
                requestId: "e4f5a6b7-c8d9-0123-ef01-4567890abcde"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "27fcc299-9b9b-4cfd-a87f-82260d7fc124"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "NOT_FOUND"
                requestId: "f5a6b7c8-d9e0-1234-f012-567890abcdef"

  # --- Conversation Member Paths ---
  /v1/conversations/{conversationId}/members:
    get:
      summary: "Get conversation members"
      description: "Lấy danh sách thành viên trong cuộc trò chuyện."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của cuộc trò chuyện"
      responses:
        '200':
          description: "Success - Members retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ConversationMemberResponse'
              example:
                success: true
                message: "Members retrieved successfully"
                data:
                  - id: 1
                    fullName: "John Doe"
                    username: "johndoe"
                    avatarUrl: "https://example.com/avatar1.jpg"
                    online: true
                  - id: 2
                    fullName: "Jane Smith"
                    username: "janesmith"
                    avatarUrl: "https://example.com/avatar2.jpg"
                    online: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-members001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-members002"

    post:
      summary: "Add members to conversation"
      description: "Thêm thành viên mới vào cuộc trò chuyện (chỉ admin mới có quyền)."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của cuộc trò chuyện"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddMembersRequest'
            example:
              userIds: [3, 4, 5]
      responses:
        '201':
          description: "Success - Members added successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/AddMembersResponse'
              example:
                success: true
                message: "Members added successfully"
                data:
                  conversationId: 1
                  addedMembers:
                    - userId: 3
                      username: "alice"
                      fullName: "Alice Johnson"
                      role: "MEMBER"
                      joinedAt: "2024-01-15T10:30:00Z"
                    - userId: 4
                      username: "bob"
                      fullName: "Bob Wilson"
                      role: "MEMBER"
                      joinedAt: "2024-01-15T10:30:00Z"
        '400':
          description: "Bad Request - Validation error or business rule violation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User IDs are required"
                code: "VALIDATION_ERROR"
                requestId: "req-addmem001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-addmem002"
        '403':
          description: "Forbidden - Only admins can add members."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Only admins can add members"
                code: "FORBIDDEN"
                requestId: "req-addmem003"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-addmem004"

  /v1/conversations/{conversationId}/members/{userId}:
    delete:
      summary: "Remove member from conversation"
      description: "Xóa thành viên khỏi cuộc trò chuyện (chỉ admin mới có quyền)."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của cuộc trò chuyện"
        - name: userId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của user cần xóa"
      responses:
        '200':
          description: "Success - Member removed successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Member removed successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-remmem001"
        '403':
          description: "Forbidden - Only admins can remove members."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Only admins can remove members"
                code: "FORBIDDEN"
                requestId: "req-remmem002"
        '404':
          description: "Not Found - Conversation or user not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found in conversation"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-remmem003"

  /v1/conversations/{conversationId}/members/leave:
    post:
      summary: "Leave conversation"
      description: "Rời khỏi cuộc trò chuyện (user tự rời)."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của cuộc trò chuyện"
      responses:
        '200':
          description: "Success - Left conversation successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Left conversation successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-leave001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-leave002"

  /v1/conversations/{conversationId}/members/{userId}/role:
    put:
      summary: "Update member role"
      description: "Cập nhật vai trò của thành viên (ADMIN hoặc MEMBER). Chỉ admin mới có quyền."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của cuộc trò chuyện"
        - name: userId
          in: path
          required: true
          schema:
            type: integer
            format: int64
          description: "ID của user cần cập nhật role"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMemberRoleRequest'
            example:
              role: "ADMIN"
      responses:
        '200':
          description: "Success - Member role updated successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationParticipantResponse'
              example:
                success: true
                message: "Member role updated successfully"
                data:
                  userId: 3
                  username: "alice"
                  role: "ADMIN"
        '400':
          description: "Bad Request - Validation error."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Role must be either ADMIN or MEMBER"
                code: "VALIDATION_ERROR"
                requestId: "req-updaterole001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-updaterole002"
        '403':
          description: "Forbidden - Only admins can update member roles."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Only admins can update member roles"
                code: "FORBIDDEN"
                requestId: "req-updaterole003"
        '404':
          description: "Not Found - Conversation or user not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found in conversation"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-updaterole004"

  # --- Conversation Settings Paths ---
  /v1/conversations/{conversationId}/settings:
    get:
      summary: "Get conversation settings"
      description: "Lấy settings của conversation cho user hiện tại."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Settings retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Settings retrieved successfully"
                data:
                  conversationId: 1
                  isMuted: false
                  notificationsEnabled: true
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-getsettings001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-getsettings002"

    put:
      summary: "Update conversation settings"
      description: "Cập nhật settings của conversation cho user hiện tại."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateConversationSettingsRequest'
            example:
              notificationsEnabled: true
              customNickname: "My Best Friend"
              theme: "blue"
      responses:
        '200':
          description: "Success - Settings updated successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Settings updated successfully"
                data:
                  conversationId: 1
                  isMuted: false
                  notificationsEnabled: true
                  customNickname: "My Best Friend"
                  theme: "blue"
        '400':
          description: "Bad Request - Validation error."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Validation failed"
                code: "VALIDATION_ERROR"
                requestId: "req-updatesettings001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-updatesettings002"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-updatesettings003"

  /v1/conversations/{conversationId}/settings/mute:
    post:
      summary: "Mute conversation"
      description: "Mute conversation để không nhận thông báo."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation muted."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation muted"
                data:
                  conversationId: 1
                  isMuted: true
                  mutedUntil: "2025-12-17T10:30:00Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-mute001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-mute002"

  /v1/conversations/{conversationId}/settings/unmute:
    post:
      summary: "Unmute conversation"
      description: "Unmute conversation để nhận lại thông báo."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation unmuted."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation unmuted"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unmute001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-unmute002"

  /v1/conversations/{conversationId}/settings/pin:
    post:
      summary: "Pin conversation"
      description: "Pin conversation để hiển thị ở đầu danh sách."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation pinned."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation pinned"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-pin001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-pin002"

  /v1/conversations/{conversationId}/settings/unpin:
    post:
      summary: "Unpin conversation"
      description: "Unpin conversation."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation unpinned."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation unpinned"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unpin001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-unpin002"

  /v1/conversations/{conversationId}/settings/archive:
    post:
      summary: "Archive conversation"
      description: "Archive conversation để ẩn khỏi danh sách chính."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation archived."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation archived"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-archive001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-archive002"

  /v1/conversations/{conversationId}/settings/unarchive:
    post:
      summary: "Unarchive conversation"
      description: "Unarchive conversation để hiển thị lại trong danh sách chính."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation unarchived."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation unarchived"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unarchive001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-unarchive002"

  /v1/conversations/{conversationId}/settings/hide:
    post:
      summary: "Hide conversation"
      description: "Hide conversation khỏi danh sách."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation hidden."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation hidden"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-hide001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-hide002"

  /v1/conversations/{conversationId}/settings/unhide:
    post:
      summary: "Unhide conversation"
      description: "Unhide conversation để hiển thị lại trong danh sách."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
      responses:
        '200':
          description: "Success - Conversation unhidden."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/ConversationSettingsResponse'
              example:
                success: true
                message: "Conversation unhidden"
                data:
                  conversationId: 1
                  isMuted: false
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unhide001"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-unhide002"

  /v1/conversations/{conversationId}/settings/block:
    post:
      summary: "Block user in conversation"
      description: "Block user trong DIRECT conversation."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation (phải là DIRECT)"
      responses:
        '200':
          description: "Success - User blocked successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/BlockUserResponse'
              example:
                success: true
                message: "User blocked successfully"
                data:
                  isBlocked: true
                  blockedAt: "2025-12-16T10:30:00Z"
        '400':
          description: "Bad Request - Cannot block in group conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Can only block users in direct conversations"
                code: "BAD_REQUEST"
                requestId: "req-block001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-block002"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-block003"

  /v1/conversations/{conversationId}/settings/unblock:
    post:
      summary: "Unblock user in conversation"
      description: "Unblock user trong DIRECT conversation."
      tags: [Conversations]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation (phải là DIRECT)"
      responses:
        '200':
          description: "Success - User unblocked successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    $ref: '#/components/schemas/BlockUserResponse'
              example:
                success: true
                message: "User unblocked successfully"
                data:
                  isBlocked: false
        '400':
          description: "Bad Request - Cannot unblock in group conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Can only unblock users in direct conversations"
                code: "BAD_REQUEST"
                requestId: "req-unblock001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unblock002"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-unblock003"

  # --- Typing Indicator Paths ---
  /v1/conversations/{conversationId}/typing/start:
    post:
      summary: "Start typing indicator"
      description: |
        Đánh dấu user hiện tại đang typing trong conversation.
        
        **Behavior:**
        - Typing indicator tự động expire sau 3 giây nếu không có activity
        - Client nên gửi request này mỗi 2-3 giây khi user đang typing
        - Khi user ngừng typing, gọi endpoint `/stop` để clear indicator ngay lập tức
        
        **WebSocket Event:**
        - Server sẽ broadcast event `typing.indicator` đến các participants khác trong conversation
      tags: [Typing Indicators]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          description: "ID của conversation"
          schema:
            type: integer
            format: int64
          example: 1
      responses:
        '200':
          description: "Typing indicator started successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Started typing"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-typing001"
        '403':
          description: "Forbidden - User is not a participant of the conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a participant of this conversation"
                code: "FORBIDDEN"
                requestId: "req-typing002"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-typing003"

  /v1/conversations/{conversationId}/typing/stop:
    post:
      summary: "Stop typing indicator"
      description: |
        Đánh dấu user hiện tại đã ngừng typing trong conversation.
        
        **Behavior:**
        - Clear typing indicator ngay lập tức
        - Nên gọi endpoint này khi user ngừng typing hoặc gửi tin nhắn
        
        **WebSocket Event:**
        - Server sẽ broadcast event `typing.indicator` đến các participants khác trong conversation
      tags: [Typing Indicators]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          description: "ID của conversation"
          schema:
            type: integer
            format: int64
          example: 1
      responses:
        '200':
          description: "Typing indicator stopped successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Stopped typing"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-typing004"
        '403':
          description: "Forbidden - User is not a participant of the conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a participant of this conversation"
                code: "FORBIDDEN"
                requestId: "req-typing005"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-typing006"

  /v1/conversations/{conversationId}/typing/status:
    get:
      summary: "Get typing users"
      description: |
        Lấy danh sách user IDs đang typing trong conversation.
        
        **Behavior:**
        - Trả về Set<Long> chứa user IDs đang typing
        - Có thể exclude current user bằng query parameter `excludeUserId`
        - Typing indicators tự động expire sau 3 giây
        
        **Note:** Thông thường client không cần poll endpoint này vì typing events được broadcast qua WebSocket.
      tags: [Typing Indicators]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          description: "ID của conversation"
          schema:
            type: integer
            format: int64
          example: 1
        - name: excludeUserId
          in: query
          required: false
          description: "User ID to exclude from results (thường là current user)"
          schema:
            type: integer
            format: int64
          example: 12
      responses:
        '200':
          description: "Typing status retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      type: integer
                      format: int64
              examples:
                with_typing_users:
                  summary: "Users are typing"
                  value:
                    success: true
                    message: "Typing status retrieved"
                    data: [17, 18]
                no_typing_users:
                  summary: "No users typing"
                  value:
                    success: true
                    message: "Typing status retrieved"
                    data: []
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-typing007"
        '403':
          description: "Forbidden - User is not a participant of the conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a participant of this conversation"
                code: "FORBIDDEN"
                requestId: "req-typing008"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-typing009"

  # --- Message Paths ---
  /v1/conversations/{conversationId}/messages:
    get:
      summary: "Get conversation messages"
      description: "Lấy danh sách tin nhắn trong một cuộc trò chuyện, có hỗ trợ phân trang."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: page
          in: query
          schema:
            type: integer
            default: 0
          description: "Số trang (bắt đầu từ 0)."
        - name: size
          in: query
          schema:
            type: integer
            default: 50
          description: "Kích thước trang."
        - name: sort
          in: query
          schema:
            type: string
            default: "DESC"
            enum: [ASC, DESC]
          description: "Thứ tự sắp xếp theo thời gian."
      responses:
        '200':
          description: "Messages retrieved successfully."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse_MessageListResponse'
              example:
                success: true
                message: "Messages retrieved successfully"
                data:
                  - id: 26
                    conversationId: 1
                    senderId: 1
                    senderUsername: "user1"
                    senderFullName: "Nguyen Linh La"
                    content: "string"
                    type: "TEXT"
                    mediaUrl: "https://nYwpAjwqVFDQdLrdyYR.wovqJONr,8x79TyP"
                    thumbnailUrl: "http://YT.zyPzpmxdkww,kZbmkW.rDO7WdbpdBKBcjZCZs-epOlNMhx1blfhT"
                    fileName: "string"
                    fileSize: 4158
                    duration: 570
                    latitude: 690.6373466177084
                    longitude: 3893.8177545502817
                    locationName: "string"
                    reactions: {}
                    sentAt: "2025-12-15T07:30:01.506942Z"
                    createdAt: "2025-12-15T07:30:01.506944Z"
                    updatedAt: "2025-12-15T07:30:01.506944Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 0
                  - id: 25
                    conversationId: 1
                    senderId: 1
                    senderUsername: "user1"
                    senderFullName: "Nguyen Linh La"
                    content: "asd"
                    type: "TEXT"
                    mediaUrl: "https://JCHiUWYvAEtwLxwJNlR.prbZR,PnuBerNo1LkhQFtlmSCc0qpTPvx8PaNDFQSVxcBB9QT2k.DyDXX2S69MUMgtG6"
                    thumbnailUrl: "http://FNE.ixvqSdGY+TpxERpZYigqTXxvkd5JBw15FbyW5lHGQ"
                    fileName: "string"
                    fileSize: 9771
                    duration: 1652
                    latitude: 1361.6448470808052
                    longitude: 1333.8445949905276
                    locationName: "string"
                    reactions: {}
                    sentAt: "2025-12-14T06:53:17.372188Z"
                    createdAt: "2025-12-14T06:53:17.372191Z"
                    updatedAt: "2025-12-14T06:53:17.372191Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 0
                  - id: 8
                    conversationId: 1
                    senderId: 2
                    senderUsername: "user2"
                    senderFullName: "Tran Van Binh"
                    content: "Tao phải đỡ cho em tao"
                    type: "TEXT"
                    reactions: {}
                    sentAt: "2025-12-14T06:29:24.691046Z"
                    createdAt: "2025-12-14T06:29:24.691048Z"
                    updatedAt: "2025-12-14T06:29:24.691049Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 0
                  - id: 7
                    conversationId: 1
                    senderId: 2
                    senderUsername: "user2"
                    senderFullName: "Tran Van Binh"
                    content: "Phía trước là con dao"
                    type: "TEXT"
                    reactions: {}
                    sentAt: "2025-12-14T06:18:47.336162Z"
                    createdAt: "2025-12-14T06:18:47.336164Z"
                    updatedAt: "2025-12-14T06:18:47.336164Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 0
                  - id: 6
                    conversationId: 1
                    senderId: 2
                    senderUsername: "user2"
                    senderFullName: "Tran Van Binh"
                    content: "Tao vẫn nhớ nét mặt nó ngày hôm đó"
                    type: "TEXT"
                    reactions: {}
                    sentAt: "2025-12-14T06:18:41.153512Z"
                    createdAt: "2025-12-14T06:18:41.153514Z"
                    updatedAt: "2025-12-14T06:18:41.153514Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 1
                    readBy:
                      - userId: 1
                        username: "user1"
                        fullName: "Nguyen Linh La"
                        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                        readAt: "2025-12-14T06:50:24.563Z"
                  - id: 5
                    conversationId: 1
                    senderId: 2
                    senderUsername: "user2"
                    senderFullName: "Tran Van Binh"
                    content: "Hì hì đúng thật, cũng đã nhiều năm trôi qua"
                    type: "TEXT"
                    reactions: {}
                    sentAt: "2025-12-14T06:18:25.432835Z"
                    createdAt: "2025-12-14T06:18:25.432836Z"
                    updatedAt: "2025-12-14T06:18:25.432837Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 1
                    readBy:
                      - userId: 1
                        username: "user1"
                        fullName: "Nguyen Linh La"
                        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                        readAt: "2025-12-14T06:50:24.557Z"
                  - id: 3
                    conversationId: 1
                    senderId: 1
                    senderUsername: "user1"
                    senderFullName: "Nguyen Linh La"
                    content: "da thay doi"
                    type: "TEXT"
                    reactions: {}
                    sentAt: "2025-12-14T06:10:21.019500Z"
                    createdAt: "2025-12-14T06:10:21.019502Z"
                    updatedAt: "2025-12-14T06:51:40.618481Z"
                    edited: true
                    editedAt: "2025-12-14T06:51:40.617583Z"
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 1
                    readBy:
                      - userId: 2
                        username: "user2"
                        fullName: "Tran Van Binh"
                        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/2.png"
                        readAt: "2025-12-14T06:15:55.734Z"
                  - id: 2
                    conversationId: 1
                    senderId: 1
                    senderUsername: "user1"
                    senderFullName: "Nguyen Linh La"
                    content: "Nhắn nhầm nha"
                    type: "TEXT"
                    reactions:
                      "😀":
                        - 2
                    sentAt: "2025-12-14T06:10:15.643792Z"
                    createdAt: "2025-12-14T06:10:15.643797Z"
                    updatedAt: "2025-12-14T06:17:27.007639Z"
                    edited: true
                    editedAt: "2025-12-14T06:11:19.085510Z"
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 1
                    readBy:
                      - userId: 2
                        username: "user2"
                        fullName: "Tran Van Binh"
                        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/2.png"
                        readAt: "2025-12-14T06:15:55.731Z"
                  - id: 1
                    conversationId: 1
                    senderId: 1
                    senderUsername: "user1"
                    senderFullName: "Nguyen Linh La"
                    content: "Hello bro"
                    type: "TEXT"
                    reactions:
                      "😀":
                        - 1
                    sentAt: "2025-12-14T06:01:41.341217Z"
                    createdAt: "2025-12-14T06:01:41.341219Z"
                    updatedAt: "2025-12-14T06:53:57.997911Z"
                    edited: false
                    deleted: false
                    forwarded: false
                    forwardCount: 0
                    readCount: 1
                    readBy:
                      - userId: 2
                        username: "user2"
                        fullName: "Tran Van Binh"
                        avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/2.png"
                        readAt: "2025-12-14T06:15:55.725Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-msgs001"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-msgs002"
        '404':
          description: "Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-msgs003"
    post:
      summary: "Send a message"
      description: "Gửi một tin nhắn mới vào cuộc trò chuyện."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatMessageRequest'
      responses:
        '201':
          description: "Message sent successfully."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse_MessageResponse'
              example:
                success: true
                message: "Message sent successfully"
                data:
                  id: 25
                  conversationId: 1
                  senderId: 1
                  senderUsername: "user1"
                  senderFullName: "Nguyen Linh La"
                  content: "Hello, how are you?"
                  type: "TEXT"
                  reactions: {}
                  sentAt: "2025-12-14T06:53:17.372188410Z"
                  createdAt: "2025-12-14T06:53:17.372191027Z"
                  updatedAt: "2025-12-14T06:53:17.372191325Z"
                  edited: false
                  deleted: false
                  forwarded: false
                  forwardCount: 0
                  readCount: 0
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                empty_content:
                  summary: "Empty message content"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      content: "Content is required"
                    requestId: "req-msg001"
                invalid_reply:
                  summary: "Reply to non-existent message"
                  value:
                    success: false
                    message: "Reply message not found: 99999"
                    code: "RESOURCE_NOT_FOUND"
                    requestId: "req-msg002"
                invalid_mentions:
                  summary: "Mention non-existent user"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      mentionedUserIds: "Mentioned user not found: 88888"
                    requestId: "req-msg003"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-msg004"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-msg005"
        '404':
          description: "Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-msg006"

  /v1/conversations/{conversationId}/messages/{messageId}:
    get:
      summary: "Get a specific message"
      description: "Lấy thông tin chi tiết của một tin nhắn cụ thể."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Message retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_MessageResponse' }
              example:
                success: true
                message: "Message retrieved successfully"
                data:
                  id: 5
                  conversationId: 1
                  senderId: 2
                  senderUsername: "user2"
                  senderFullName: "Tran Van Binh"
                  content: "Hì hì đúng thật, cũng đã nhiều năm trôi qua"
                  type: "TEXT"
                  reactions: {}
                  sentAt: "2025-12-14T06:18:25.432835Z"
                  createdAt: "2025-12-14T06:18:25.432836Z"
                  updatedAt: "2025-12-14T06:18:25.432837Z"
                  edited: false
                  deleted: false
                  forwarded: false
                  forwardCount: 0
                  readCount: 1
                  readBy:
                    - userId: 1
                      username: "user1"
                      fullName: "Nguyen Linh La"
                      avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                      readAt: "2025-12-14T06:50:24.557Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-getmsg001"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-getmsg002"
        '404':
          description: "Message or Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                message_not_found:
                  summary: "Message not found"
                  value:
                    success: false
                    message: "Message not found"
                    code: "RESOURCE_NOT_FOUND"
                    requestId: "req-getmsg003"
                conversation_not_found:
                  summary: "Conversation not found"
                  value:
                    success: false
                    message: "Conversation not found"
                    code: "RESOURCE_NOT_FOUND"
                    requestId: "req-getmsg004"
    put:
      summary: "Update a message"
      description: "Cập nhật nội dung của một tin nhắn đã gửi. Chỉ người gửi mới có thể sửa tin nhắn."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/EditMessageRequest' }
      responses:
        '200':
          description: "Message updated successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_MessageResponse' }
              example:
                success: true
                message: "Message edited successfully"
                data:
                  id: 3
                  conversationId: 1
                  senderId: 1
                  senderUsername: "user1"
                  senderFullName: "Nguyen Linh La"
                  content: "da thay doi"
                  type: "TEXT"
                  sentAt: "2025-12-14T06:10:21.019500Z"
                  edited: true
                  editedAt: "2025-12-14T06:51:40.617582834Z"
                  deleted: false
                  forwarded: false
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                empty_content:
                  summary: "Empty message content"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      content: "Content is required"
                    requestId: "req-editmsg001"
                message_deleted:
                  summary: "Cannot edit deleted message"
                  value:
                    success: false
                    message: "Cannot edit a deleted message"
                    code: "BUSINESS_ERROR"
                    requestId: "req-editmsg002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-editmsg003"
        '403':
          description: "Forbidden - Not the message sender."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You can only edit your own messages"
                code: "FORBIDDEN"
                requestId: "req-editmsg004"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-editmsg005"
    delete:
      summary: "Delete a message"
      description: "Xóa một tin nhắn đã gửi. Chỉ người gửi mới có thể xóa tin nhắn."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Message deleted successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse' }
              example:
                success: true
                message: "Message deleted successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-delmsg001"
        '403':
          description: "Forbidden - Not the message sender."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You can only delete your own messages"
                code: "FORBIDDEN"
                requestId: "req-delmsg002"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-delmsg003"

  # --- Message Reactions ---
  /v1/messages/{messageId}/reactions:
    post:
      summary: "Add or toggle reaction"
      description: "Thêm hoặc toggle reaction (emoji) vào tin nhắn."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AddReactionRequest' }
      responses:
        '200':
          description: "Reaction toggled successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_MessageReactionsResponse' }
              example:
                success: true
                message: "Reaction toggled successfully"
                data:
                  messageId: 1
                  reactions:
                    "😀":
                      - 1
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Validation failed"
                code: "VALIDATION_ERROR"
                details:
                  emoji: "Emoji is required"
                requestId: "req-react001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-react002"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-react003"
    get:
      summary: "Get message reactions"
      description: "Lấy tất cả reactions của một tin nhắn."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Reactions retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_MessageReactionsResponse' }
              example:
                success: true
                message: "Reactions retrieved successfully"
                data:
                  messageId: 1
                  reactions:
                    "😀":
                      - 1
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-getreact001"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-getreact002"

  /v1/messages/{messageId}/reactions/{emoji}:
    delete:
      summary: "Remove reaction"
      description: "Xóa reaction của user hiện tại khỏi tin nhắn."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: emoji
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: "Reaction removed successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_MessageReactionsResponse' }
              example:
                success: true
                message: "Reaction removed successfully"
                data:
                  messageId: 1
                  reactions: {}
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-delreact001"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-delreact002"

  /v1/conversations/{conversationId}/messages/{messageId}/history:
    get:
      summary: "Get message edit history"
      description: "Lấy lịch sử chỉnh sửa của một tin nhắn."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Edit history retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: integer, format: int64 }
                        messageId: { type: integer, format: int64 }
                        previousContent: { type: string }
                        editedAt: { type: string, format: date-time }
              example:
                success: true
                message: "Edit history retrieved"
                data:
                  - id: 1
                    messageId: 5
                    previousContent: "Old content"
                    editedAt: "2025-12-14T10:00:00.000Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-msghist001"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-msghist002"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-msghist003"

  /v1/conversations/{conversationId}/messages/forward:
    post:
      summary: "Forward message"
      description: "Forward một tin nhắn đến các conversations khác."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ForwardMessageRequest' }
            example:
              messageId: 10
              conversationIds: [2, 3, 5]
      responses:
        '201':
          description: "Message forwarded successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MessageResponse'
              example:
                success: true
                message: "Message forwarded successfully"
                data: []
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                empty_conversations:
                  summary: "No target conversations"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      conversationIds: "At least one conversation is required"
                    requestId: "req-fwd001"
                invalid_conversation:
                  summary: "Target conversation not found"
                  value:
                    success: false
                    message: "Conversation not found"
                    code: "RESOURCE_NOT_FOUND"
                    requestId: "req-fwd002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-fwd003"
        '403':
          description: "Forbidden - Not a member of source or target conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-fwd004"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-fwd005"

  /v1/conversations/{conversationId}/messages/pinned:
    get:
      summary: "Get pinned messages"
      description: "Lấy danh sách tin nhắn đã pin trong conversation."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Pinned messages retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: integer, format: int64 }
                        message: { $ref: '#/components/schemas/MessageResponse' }
                        pinnedBy: { type: integer, format: int64 }
                        pinnedAt: { type: string, format: date-time }
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-pinned001"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-pinned002"
        '404':
          description: "Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-pinned003"

  /v1/conversations/{conversationId}/messages/{messageId}/pin:
    post:
      summary: "Pin message"
      description: "Pin một tin nhắn trong conversation."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '201':
          description: "Message pinned successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: object
                    properties:
                      id: { type: integer, format: int64 }
                      messageId: { type: integer, format: int64 }
                      pinnedBy: { type: integer, format: int64 }
                      pinnedAt: { type: string, format: date-time }
        '400':
          description: "Bad Request - Message already pinned."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Message is already pinned"
                code: "BAD_REQUEST"
                requestId: "req-pin001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-pin002"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-pin003"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-pin004"
    delete:
      summary: "Unpin message"
      description: "Bỏ pin một tin nhắn trong conversation."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Message unpinned successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Message unpinned successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unpin001"
        '403':
          description: "Forbidden - Not a member of this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You are not a member of this conversation"
                code: "FORBIDDEN"
                requestId: "req-unpin002"
        '404':
          description: "Pinned message not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Pinned message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-unpin003"

  /v1/conversations/{conversationId}/messages/search:
    get:
      summary: "Search messages in conversation"
      description: "Tìm kiếm tin nhắn trong conversation theo query, type, sender."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: query
          in: query
          required: false
          schema: { type: string }
          description: "Search query for message content"
        - name: type
          in: query
          required: false
          schema: { type: string, enum: [TEXT, IMAGE, VIDEO, VOICE, AUDIO, DOCUMENT, LOCATION, SYSTEM] }
          description: "Filter by message type"
        - name: senderId
          in: query
          required: false
          schema: { type: integer, format: int64 }
          description: "Filter by sender ID"
        - name: page
          in: query
          required: false
          schema: { type: integer, default: 0 }
        - name: size
          in: query
          required: false
          schema: { type: integer, default: 20 }
        - name: sort
          in: query
          required: false
          schema: { type: string, enum: [ASC, DESC], default: DESC }
      responses:
        '200':
          description: "Messages searched successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: object
                    properties:
                      messages:
                        type: array
                        items: { $ref: '#/components/schemas/MessageResponse' }
                      page: { type: integer }
                      size: { type: integer }
                      total: { type: integer, format: int64 }
                      totalPages: { type: integer }
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-search-401"
        '403':
          description: "Forbidden - You do not have access to this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You do not have access to this conversation"
                code: "FORBIDDEN"
                requestId: "req-search-403"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-search-404"

  /v1/conversations/{conversationId}/media:
    get:
      summary: "Get media files from conversation"
      description: "Lấy danh sách media files (images, videos, files) từ conversation."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
        - name: type
          in: query
          required: false
          schema: { type: string, enum: [IMAGE, VIDEO, FILE, AUDIO] }
          description: "Filter by media type"
        - name: page
          in: query
          required: false
          schema: { type: integer, default: 0 }
        - name: size
          in: query
          required: false
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: "Media files retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: object
                    properties:
                      messages:
                        type: array
                        items: { $ref: '#/components/schemas/MessageResponse' }
                      page: { type: integer }
                      size: { type: integer }
                      total: { type: integer, format: int64 }
                      totalPages: { type: integer }
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-media-401"
        '403':
          description: "Forbidden - You do not have access to this conversation."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "You do not have access to this conversation"
                code: "FORBIDDEN"
                requestId: "req-media-403"
        '404':
          description: "Not Found - Conversation not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-media-404"

  # --- Read Receipts ---
  /v1/read-receipts/messages/{messageId}:
    post:
      summary: "Mark message as read"
      description: "Đánh dấu một tin nhắn cụ thể là đã đọc. Tự động tạo read receipt."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của tin nhắn cần đánh dấu đã đọc"
      responses:
        '204':
          description: "Message marked as read successfully (no content)."
        '400':
          description: "Bad Request - Not a participant in this conversation."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "You are not a participant in this conversation"
                code: "BAD_REQUEST"
                requestId: "req-read001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-read002"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-read003"
    get:
      summary: "Get read receipts for message"
      description: "Lấy danh sách người dùng đã đọc tin nhắn này, bao gồm thời gian đọc."
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của tin nhắn"
      responses:
        '200':
          description: "Read receipts retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ReadReceiptListResponse' }
              example:
                success: true
                message: "Read receipts retrieved successfully"
                data:
                  - userId: 1
                    username: "user1"
                    fullName: "Nguyen Linh La"
                    avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                    readAt: "2025-12-14T06:50:24.557Z"
                  - userId: 3
                    username: "hoale"
                    fullName: "Le Thi Hoa"
                    avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/3.png"
                    readAt: "2025-12-14T07:15:30.123Z"
        '400':
          description: "Bad Request - Not a participant in this conversation."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "You are not a participant in this conversation"
                code: "BAD_REQUEST"
                requestId: "req-getread001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-getread002"
        '404':
          description: "Message not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Message not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-getread003"

  /v1/read-receipts/conversations/{conversationId}:
    post:
      summary: "Mark conversation as read"
      description: |
        Đánh dấu tất cả tin nhắn trong conversation là đã đọc.
        
        **Features:**
        - Tạo read receipts cho tất cả tin nhắn chưa đọc
        - Bỏ qua tin nhắn của chính user
        - Bỏ qua tin nhắn đã đọc rồi
        - Reset unread count trong participant table
        - Có thể chỉ định lastMessageId để chỉ đánh dấu đến tin nhắn đó
      tags: [Messages]
      security:
        - bearerAuth: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của conversation"
        - name: lastMessageId
          in: query
          required: false
          schema: { type: integer, format: int64 }
          description: "ID của tin nhắn cuối cùng cần đánh dấu đã đọc. Nếu không có, sẽ đánh dấu tất cả."
      responses:
        '204':
          description: "Conversation marked as read successfully (no content)."
        '400':
          description: "Bad Request - Not a participant in this conversation."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "You are not a participant in this conversation"
                code: "BAD_REQUEST"
                requestId: "req-convread001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-convread002"
        '404':
          description: "Conversation not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Conversation not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-convread003"

  /v1/read-receipts/unread-count:
    get:
      summary: "Get global unread count"
      description: |
        Lấy tổng số tin nhắn chưa đọc từ tất cả conversations của user hiện tại.
        
        **Use case:** Badge notification trên app icon hoặc tab.
      tags: [Messages]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Unread count retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string, example: "Unread count retrieved" }
                  data:
                    type: object
                    properties:
                      unreadCount: { type: integer, format: int64, description: "Total unread messages across all conversations" }
              example:
                success: true
                message: "Unread count retrieved"
                data:
                  unreadCount: 15
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-unread001"

  # --- Call Paths ---
  /v1/calls/initiate:
    post:
      summary: "Initiate a new call"
      description: |
        Bắt đầu một cuộc gọi đến một người dùng khác.
        
        **Flow:**
        1. Validate cả caller và callee không busy
        2. Tạo Call record với status RINGING
        3. Generate Agora token cho caller
        4. Send WebSocket invitation tới callee
        5. Schedule timeout (30s)
        
        **Note:** User ID của caller được lấy từ JWT token.
      tags: [Call]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/InitiateCallRequest' }
            examples:
              audio_call:
                summary: "Audio call"
                value:
                  calleeId: 10
                  callType: "AUDIO"
              video_call:
                summary: "Video call"
                value:
                  calleeId: 15
                  callType: "VIDEO"
      responses:
        '201':
          description: "Call initiated successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_CallConnectionResponse' }
              example:
                success: true
                message: "Call initiated"
                data:
                  callInfo:
                    id: "call-uuid-123"
                    channelId: "channel_1734160800000_1_10"
                    status: "RINGING"
                    callType: "VIDEO"
                    callerId: 1
                    callerName: "Nguyen Linh La"
                    callerAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                    calleeId: 10
                    calleeName: "Nguyen Van A"
                    calleeAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                    createdAt: "2025-12-14T10:00:00.000Z"
                    durationSeconds: null
                  token: "agora_token_here_for_caller"
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                user_busy:
                  summary: "User is busy"
                  value:
                    success: false
                    message: "User is busy in another call"
                    code: "USER_BUSY"
                    requestId: "req-call001"
                missing_callee_id:
                  summary: "Missing calleeId"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      calleeId: "Callee ID is required"
                    requestId: "req-call002"
                missing_call_type:
                  summary: "Missing callType"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      callType: "Call type is required"
                    requestId: "req-call003"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-call004"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-call005"

  /v1/calls/{callId}/accept:
    post:
      summary: "Accept a call"
      description: |
        Người nhận chấp nhận một cuộc gọi đến.
        
        **Flow:**
        1. Validate user là callee
        2. Validate call status là RINGING
        3. Update status = CONNECTED
        4. Cancel timeout scheduler
        5. Generate Agora token cho callee
        6. Send WebSocket notification tới caller
        
        **Note:** Chỉ callee mới có thể accept call.
      tags: [Call]
      security:
        - bearerAuth: []
      parameters:
        - name: callId
          in: path
          required: true
          schema: { type: string }
          description: "UUID của call"
          example: "call-uuid-123"
      responses:
        '200':
          description: "Call accepted successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_CallConnectionResponse' }
              example:
                success: true
                message: "Call accepted"
                data:
                  callInfo:
                    id: "call-uuid-123"
                    channelId: "channel_1734160800000_1_10"
                    status: "CONNECTED"
                    callType: "VIDEO"
                    callerId: 1
                    callerName: "Nguyen Linh La"
                    callerAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                    calleeId: 10
                    calleeName: "Nguyen Van A"
                    calleeAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                    createdAt: "2025-12-14T10:00:00.000Z"
                    durationSeconds: null
                  token: "agora_token_here_for_callee"
        '400':
          description: "Bad Request - Invalid status."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Call is not ringing"
                code: "INVALID_STATUS"
                requestId: "req-accept001"
        '401':
          description: "Unauthorized - Not the callee or invalid token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                not_callee:
                  summary: "Not the callee"
                  value:
                    success: false
                    message: "Only callee can accept this call"
                    code: "UNAUTHORIZED"
                    requestId: "req-accept002"
                invalid_token:
                  summary: "Invalid token"
                  value:
                    success: false
                    message: "Invalid or expired token"
                    code: "UNAUTHORIZED"
                    requestId: "req-accept003"
        '404':
          description: "Call not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Call not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-accept004"

  /v1/calls/{callId}/reject:
    post:
      summary: "Reject a call"
      description: |
        Người nhận từ chối một cuộc gọi đến.
        
        **Flow:**
        1. Validate user là callee
        2. Validate call status là RINGING
        3. Update status = REJECTED
        4. Cancel timeout scheduler
        5. Send WebSocket notification tới caller
        
        **Reasons:** busy, declined, unavailable
        
        **Note:** Chỉ callee mới có thể reject call.
      tags: [Call]
      security:
        - bearerAuth: []
      parameters:
        - name: callId
          in: path
          required: true
          schema: { type: string }
          description: "UUID của call"
          example: "call-uuid-123"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RejectCallRequest' }
            examples:
              busy:
                summary: "User is busy"
                value:
                  reason: "busy"
              declined:
                summary: "User declined"
                value:
                  reason: "declined"
              unavailable:
                summary: "User unavailable"
                value:
                  reason: "unavailable"
      responses:
        '200':
          description: "Call rejected successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_CallResponse' }
              example:
                success: true
                message: "Call rejected"
                data:
                  id: "call-uuid-123"
                  channelId: "channel_1734160800000_1_10"
                  status: "REJECTED"
                  callType: "VIDEO"
                  callerId: 1
                  callerName: "Nguyen Linh La"
                  callerAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                  calleeId: 10
                  calleeName: "Nguyen Van A"
                  calleeAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                  createdAt: "2025-12-14T10:00:00.000Z"
                  durationSeconds: null
        '400':
          description: "Bad Request - Invalid status or reason."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                invalid_status:
                  summary: "Call not ringing"
                  value:
                    success: false
                    message: "Cannot reject call"
                    code: "INVALID_STATUS"
                    requestId: "req-reject001"
                invalid_reason:
                  summary: "Invalid reason"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      reason: "Reason must be one of: busy, declined, unavailable"
                    requestId: "req-reject002"
                missing_reason:
                  summary: "Missing reason"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      reason: "Reason cannot be blank"
                    requestId: "req-reject003"
        '401':
          description: "Unauthorized - Not the callee or invalid token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                not_callee:
                  summary: "Not the callee"
                  value:
                    success: false
                    message: "Only callee can reject this call"
                    code: "UNAUTHORIZED"
                    requestId: "req-reject004"
                invalid_token:
                  summary: "Invalid token"
                  value:
                    success: false
                    message: "Invalid or expired token"
                    code: "UNAUTHORIZED"
                    requestId: "req-reject005"
        '404':
          description: "Call not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Call not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-reject006"

  /v1/calls/{callId}/end:
    post:
      summary: "End a call"
      description: |
        Một trong hai bên kết thúc cuộc gọi.
        
        **Flow:**
        1. Validate user là caller hoặc callee
        2. Validate call chưa ended
        3. Update status = ENDED
        4. Calculate duration
        5. Cancel timeout scheduler
        6. Send WebSocket notification tới partner
        
        **Reasons:** hangup (default), network error, device error, timeout
        
        **Note:** Cả caller và callee đều có thể end call.
      tags: [Call]
      security:
        - bearerAuth: []
      parameters:
        - name: callId
          in: path
          required: true
          schema: { type: string }
          description: "UUID của call"
          example: "call-uuid-123"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/EndCallRequest' }
            examples:
              normal_hangup:
                summary: "Normal hangup"
                value:
                  reason: "hangup"
              network_error:
                summary: "Network error"
                value:
                  reason: "network error"
              device_error:
                summary: "Device error"
                value:
                  reason: "device error"
              timeout:
                summary: "Timeout"
                value:
                  reason: "timeout"
      responses:
        '200':
          description: "Call ended successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_CallResponse' }
              example:
                success: true
                message: "Call ended"
                data:
                  id: "call-uuid-123"
                  channelId: "channel_1734160800000_1_10"
                  status: "ENDED"
                  callType: "VIDEO"
                  callerId: 1
                  callerName: "Nguyen Linh La"
                  callerAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/1.png"
                  calleeId: 10
                  calleeName: "Nguyen Van A"
                  calleeAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                  createdAt: "2025-12-14T10:00:00.000Z"
                  durationSeconds: 180
        '400':
          description: "Bad Request - Call already ended or invalid reason."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  details:
                    type: object
                    additionalProperties:
                      type: string
                  requestId: { type: string }
              examples:
                already_ended:
                  summary: "Call already ended"
                  value:
                    success: false
                    message: "Call already ended"
                    code: "CALL_ALREADY_ENDED"
                    requestId: "req-end001"
                invalid_reason:
                  summary: "Invalid reason"
                  value:
                    success: false
                    message: "Validation failed"
                    code: "VALIDATION_ERROR"
                    details:
                      reason: "Reason must be one of: hangup, network error, device error, timeout"
                    requestId: "req-end002"
        '401':
          description: "Unauthorized - Not a participant or invalid token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                not_participant:
                  summary: "Not a participant"
                  value:
                    success: false
                    message: "You are not a participant of this call"
                    code: "UNAUTHORIZED"
                    requestId: "req-end003"
                invalid_token:
                  summary: "Invalid token"
                  value:
                    success: false
                    message: "Invalid or expired token"
                    code: "UNAUTHORIZED"
                    requestId: "req-end004"
        '404':
          description: "Call not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Call not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-end005"

  /v1/calls/history:
    get:
      summary: "Get call history"
      description: |
        Retrieve paginated call history for the current user with optional filters.
        
        **Query Parameters:**
        - `page`: Page number (0-indexed, default: 0)
        - `size`: Page size (default: 20)
        - `callType`: Filter by call type (AUDIO or VIDEO)
        - `status`: Filter by call status (COMPLETED, MISSED, REJECTED, FAILED)
        
        **Note:** User ID is extracted from JWT token.
      tags: [Call]
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            format: int32
            default: 0
          description: "Page number (0-indexed)"
          example: 0
        - name: size
          in: query
          required: false
          schema:
            type: integer
            format: int32
            default: 20
          description: "Number of items per page"
          example: 20
        - name: callType
          in: query
          required: false
          schema:
            type: string
            enum: [AUDIO, VIDEO]
          description: "Filter by call type"
          example: "VIDEO"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [COMPLETED, MISSED, REJECTED, FAILED]
          description: "Filter by call status"
          example: "COMPLETED"
      responses:
        '200':
          description: "Call history retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_CallHistoryResponse' }
              example:
                success: true
                message: "Call history retrieved successfully"
                data:
                  data:
                    - id: "hist-001"
                      callId: "call-uuid-123"
                      remoteUserId: "10"
                      remoteUserName: "Nguyen Van A"
                      remoteUserAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                      callType: "VIDEO"
                      status: "COMPLETED"
                      direction: "OUTGOING"
                      timestamp: "2025-12-14T10:00:00.000Z"
                      durationSeconds: 180
                    - id: "hist-002"
                      callId: "call-uuid-456"
                      remoteUserId: "15"
                      remoteUserName: "Le Thi C"
                      remoteUserAvatar: null
                      callType: "AUDIO"
                      status: "MISSED"
                      direction: "INCOMING"
                      timestamp: "2025-12-13T15:30:00.000Z"
                      durationSeconds: null
                    - id: "hist-003"
                      callId: "call-uuid-789"
                      remoteUserId: "20"
                      remoteUserName: "Tran Van B"
                      remoteUserAvatar: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/20.png"
                      callType: "VIDEO"
                      status: "REJECTED"
                      direction: "OUTGOING"
                      timestamp: "2025-12-12T09:15:00.000Z"
                      durationSeconds: null
                  page: 0
                  size: 20
                  total: 45
                  totalPages: 3
                  hasNextPage: true
                  hasPrevPage: false
        '400':
          description: "Bad Request - Invalid query parameters."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                invalid_call_type:
                  summary: "Invalid callType"
                  value:
                    success: false
                    message: "Invalid callType: INVALID"
                    code: "BAD_REQUEST"
                    requestId: "req-history001"
                invalid_status:
                  summary: "Invalid status"
                  value:
                    success: false
                    message: "Invalid status: INVALID"
                    code: "BAD_REQUEST"
                    requestId: "req-history002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-history003"

  /v1/calls/history/{callId}:
    delete:
      summary: "Delete call history entry"
      description: |
        Delete a specific call history entry for the current user.
        
        **Note:** Only the owner of the call history entry can delete it.
      tags: [Call]
      security:
        - bearerAuth: []
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
          description: "Call ID of the history entry to delete"
          example: "call-uuid-123"
      responses:
        '200':
          description: "Call history deleted successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { type: object, nullable: true }
              example:
                success: true
                message: "Call history deleted successfully"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-history-del001"
        '404':
          description: "Call history not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Call history not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-history-del002"

  # --- Contacts Paths ---
  /v1/contacts:
    get:
      summary: "Get all contacts"
      description: "Lấy danh sách tất cả contacts của người dùng hiện tại."
      tags: [Contacts]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Contacts retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ContactListResponse' }
              example:
                success: true
                message: "Contacts retrieved successfully"
                data:
                  - id: 5
                    contactUserId: 10
                    username: "user10"
                    fullName: "Nguyen Van A"
                    avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                    nickname: "Best Friend"
                    favorite: true
                    online: true
                    lastSeen: null
                    createdAt: "2025-12-10T10:20:30.456Z"
                  - id: 8
                    contactUserId: 15
                    username: "user15"
                    fullName: "Le Thi C"
                    avatarUrl: null
                    nickname: null
                    favorite: false
                    online: false
                    lastSeen: "2025-12-14T06:30:00.000Z"
                    createdAt: "2025-12-11T14:15:20.123Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-contacts001"
    post:
      summary: "Add a new contact"
      description: "Thêm một contact mới vào danh bạ."
      tags: [Contacts]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AddContactRequest' }
            example:
              contactUserId: 25
              nickname: "My Colleague"
      responses:
        '201':
          description: "Contact added successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ContactResponse' }
              example:
                success: true
                message: "Contact added successfully"
                data:
                  id: 12
                  contactUserId: 25
                  username: "user25"
                  fullName: "Pham Van D"
                  avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/25.png"
                  nickname: "My Colleague"
                  favorite: false
                  online: true
                  lastSeen: null
                  createdAt: "2025-12-14T09:00:00.000Z"
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              examples:
                add_yourself:
                  summary: "Cannot add yourself"
                  value:
                    success: false
                    message: "Cannot add yourself as contact"
                    code: "BAD_REQUEST"
                    requestId: "req-addcontact001"
                already_exists:
                  summary: "Contact already exists"
                  value:
                    success: false
                    message: "Contact already exists"
                    code: "BAD_REQUEST"
                    requestId: "req-addcontact002"
                missing_user_id:
                  summary: "Missing contactUserId"
                  value:
                    success: false
                    message: "Contact user ID is required"
                    code: "BAD_REQUEST"
                    requestId: "req-addcontact003"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-addcontact004"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string }
                  code: { type: string }
                  requestId: { type: string }
              example:
                success: false
                message: "User not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-addcontact005"

  /v1/contacts/favorites:
    get:
      summary: "Get favorite contacts"
      description: "Lấy danh sách contacts yêu thích (đã đánh dấu favorite)."
      tags: [Contacts]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Favorite contacts retrieved successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ContactListResponse' }
              example:
                success: true
                message: "Favorite contacts retrieved successfully"
                data:
                  - id: 5
                    contactUserId: 10
                    username: "user10"
                    fullName: "Nguyen Van A"
                    avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                    nickname: "Best Friend"
                    favorite: true
                    online: true
                    lastSeen: null
                    createdAt: "2025-12-10T10:20:30.456Z"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-favcontacts001"

  /v1/contacts/{contactId}:
    put:
      summary: "Update a contact"
      description: "Cập nhật thông tin contact (nickname, favorite status)."
      tags: [Contacts]
      security:
        - bearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của contact cần update"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateContactRequest' }
            examples:
              update_nickname:
                summary: "Update nickname"
                value:
                  nickname: "New Nickname"
              mark_favorite:
                summary: "Mark as favorite"
                value:
                  isFavorite: true
              update_both:
                summary: "Update both"
                value:
                  nickname: "VIP Friend"
                  isFavorite: true
      responses:
        '200':
          description: "Contact updated successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse_ContactResponse' }
              example:
                success: true
                message: "Contact updated successfully"
                data:
                  id: 5
                  contactUserId: 10
                  username: "user10"
                  fullName: "Nguyen Van A"
                  avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/10.png"
                  nickname: "VIP Friend"
                  favorite: true
                  online: true
                  lastSeen: null
                  createdAt: "2025-12-10T10:20:30.456Z"
        '400':
          description: "Bad Request - No access to this contact."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "You do not have access to this contact"
                code: "BAD_REQUEST"
                requestId: "req-updatecontact001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-updatecontact002"
        '404':
          description: "Contact not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Contact not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-updatecontact003"
    delete:
      summary: "Delete a contact"
      description: "Xóa một contact khỏi danh bạ."
      tags: [Contacts]
      security:
        - bearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của contact cần xóa"
      responses:
        '200':
          description: "Contact deleted successfully."
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiResponse' }
              example:
                success: true
                message: "Contact deleted successfully"
        '400':
          description: "Bad Request - No access to this contact."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "You do not have access to this contact"
                code: "BAD_REQUEST"
                requestId: "req-deletecontact001"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-deletecontact002"
        '404':
          description: "Contact not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Contact not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-deletecontact003"

  # --- Friend Requests Paths ---
  /v1/friend-requests:
    post:
      summary: "Send friend request"
      description: |
        Gửi lời mời kết bạn đến một người dùng.
        
        **Status Flow:**
        1. PENDING - Request được gửi, chờ accept/reject
        2. ACCEPTED - Request được chấp nhận, tạo bidirectional contact
        3. REJECTED - Request bị từ chối
        
        **Note:** Không thể gửi request nếu:
        - Đã có contact relationship (PENDING/ACCEPTED/BLOCKED)
        - Receiver là chính mình
      tags: [Social]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SendFriendRequestRequest' }
            example:
              receiverUserId: 30
              nickname: "Potential Friend"
      responses:
        '201':
          description: "Friend request sent successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/FriendRequestResponse' }
              example:
                success: true
                message: "Friend request sent successfully"
                data:
                  id: 25
                  userId: 30
                  username: "user30"
                  fullName: "Hoang Van E"
                  avatarUrl: null
                  status: "PENDING"
                  nickname: "Potential Friend"
                  online: false
                  requestedAt: "2025-12-14T09:30:00.000Z"
                  acceptedAt: null
                  rejectedAt: null
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                send_to_yourself:
                  summary: "Cannot send to yourself"
                  value:
                    success: false
                    message: "Cannot send friend request to yourself"
                    code: "BAD_REQUEST"
                    requestId: "req-friendreq001"
                already_sent:
                  summary: "Already sent"
                  value:
                    success: false
                    message: "Friend request already sent"
                    code: "BAD_REQUEST"
                    requestId: "req-friendreq002"
                already_friends:
                  summary: "Already friends"
                  value:
                    success: false
                    message: "Already friends"
                    code: "BAD_REQUEST"
                    requestId: "req-friendreq003"
                user_blocked:
                  summary: "User is blocked"
                  value:
                    success: false
                    message: "Cannot send friend request to blocked user"
                    code: "BAD_REQUEST"
                    requestId: "req-friendreq004"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-friendreq005"
        '404':
          description: "User not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Receiver not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-friendreq006"

  /v1/friend-requests/received:
    get:
      summary: "Get pending friend requests received"
      description: |
        Lấy danh sách lời mời kết bạn mà user hiện tại đã nhận (chưa accept/reject).
        
        **Use case:** Hiển thị notification "X wants to be your friend"
      tags: [Social]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Pending requests retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/FriendRequestResponse'
              example:
                success: true
                message: "Pending requests retrieved successfully"
                data:
                  - id: 20
                    userId: 25
                    username: "user25"
                    fullName: "Pham Van D"
                    avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/25.png"
                    status: "PENDING"
                    nickname: null
                    online: true
                    requestedAt: "2025-12-14T08:00:00.000Z"
                    acceptedAt: null
                    rejectedAt: null
                  - id: 22
                    userId: 28
                    username: "user28"
                    fullName: "Nguyen Thi F"
                    avatarUrl: null
                    status: "PENDING"
                    nickname: null
                    online: false
                    requestedAt: "2025-12-14T07:30:00.000Z"
                    acceptedAt: null
                    rejectedAt: null
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-friendreqrecv001"

  /v1/friend-requests/sent:
    get:
      summary: "Get pending friend requests sent"
      description: |
        Lấy danh sách lời mời kết bạn mà user hiện tại đã gửi (chưa được accept/reject).
        
        **Use case:** Hiển thị danh sách "Pending requests" để có thể cancel
      tags: [Social]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "Sent requests retrieved successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/FriendRequestResponse'
              example:
                success: true
                message: "Sent requests retrieved successfully"
                data:
                  - id: 18
                    userId: 22
                    username: "user22"
                    fullName: "Tran Van G"
                    avatarUrl: null
                    status: "PENDING"
                    nickname: "Work Colleague"
                    online: false
                    requestedAt: "2025-12-14T06:00:00.000Z"
                    acceptedAt: null
                    rejectedAt: null
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-friendreqsent001"

  /v1/friend-requests/{requestId}/accept:
    post:
      summary: "Accept friend request"
      description: |
        Chấp nhận lời mời kết bạn.
        
        **Action:**
        - Set request status = ACCEPTED
        - Tạo reverse contact (bidirectional friendship)
        - Both users become friends
      tags: [Social]
      security:
        - bearerAuth: []
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của friend request cần accept"
      responses:
        '200':
          description: "Friend request accepted successfully."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string }
                  data: { $ref: '#/components/schemas/FriendRequestResponse' }
              example:
                success: true
                message: "Friend request accepted successfully"
                data:
                  id: 20
                  userId: 25
                  username: "user25"
                  fullName: "Pham Van D"
                  avatarUrl: "https://res.cloudinary.com/dk3gud5kq/image/upload/v1765476687/avatars/25.png"
                  status: "ACCEPTED"
                  nickname: null
                  online: true
                  requestedAt: "2025-12-14T08:00:00.000Z"
                  acceptedAt: "2025-12-14T09:45:00.000Z"
                  rejectedAt: null
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                not_receiver:
                  summary: "Not the receiver"
                  value:
                    success: false
                    message: "You are not the receiver of this request"
                    code: "BAD_REQUEST"
                    requestId: "req-acceptreq001"
                not_pending:
                  summary: "Request not pending"
                  value:
                    success: false
                    message: "Friend request is not pending"
                    code: "BAD_REQUEST"
                    requestId: "req-acceptreq002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-acceptreq003"
        '404':
          description: "Request not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Friend request not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-acceptreq004"

  /v1/friend-requests/{requestId}/reject:
    post:
      summary: "Reject friend request"
      description: |
        Từ chối lời mời kết bạn.
        
        **Action:**
        - Set request status = REJECTED
        - Keep the record for history (not deleted)
      tags: [Social]
      security:
        - bearerAuth: []
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của friend request cần reject"
      responses:
        '204':
          description: "Friend request rejected successfully (no content)."
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                not_receiver:
                  summary: "Not the receiver"
                  value:
                    success: false
                    message: "You are not the receiver of this request"
                    code: "BAD_REQUEST"
                    requestId: "req-rejectreq001"
                not_pending:
                  summary: "Request not pending"
                  value:
                    success: false
                    message: "Friend request is not pending"
                    code: "BAD_REQUEST"
                    requestId: "req-rejectreq002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-rejectreq003"
        '404':
          description: "Request not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Friend request not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-rejectreq004"

  /v1/friend-requests/{requestId}:
    delete:
      summary: "Cancel friend request"
      description: |
        Hủy lời mời kết bạn đã gửi (chỉ sender mới có thể cancel).
        
        **Action:**
        - Delete the request record completely
        - Only PENDING requests can be cancelled
      tags: [Social]
      security:
        - bearerAuth: []
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer, format: int64 }
          description: "ID của friend request cần cancel"
      responses:
        '204':
          description: "Friend request cancelled successfully (no content)."
        '400':
          description: "Bad Request - Validation failed."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                not_sender:
                  summary: "Not the sender"
                  value:
                    success: false
                    message: "You are not the sender of this request"
                    code: "BAD_REQUEST"
                    requestId: "req-cancelreq001"
                not_pending:
                  summary: "Request not pending"
                  value:
                    success: false
                    message: "Friend request is not pending"
                    code: "BAD_REQUEST"
                    requestId: "req-cancelreq002"
        '401':
          description: "Unauthorized - Invalid or expired token."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Invalid or expired token"
                code: "UNAUTHORIZED"
                requestId: "req-cancelreq003"
        '404':
          description: "Request not found."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                message: "Friend request not found"
                code: "RESOURCE_NOT_FOUND"
                requestId: "req-cancelreq004"

